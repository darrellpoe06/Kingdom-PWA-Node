// SCRATCH, DELIBERATELY RED (DR-0645 proof). Lives only on the throwaway branch
// claude/scratch-red-shard-proof, which is deleted after the run is recorded.
import { it, expect } from 'vitest';
it('a red shard must turn the required check red', () => {
  expect(1).toBe(2);
});
