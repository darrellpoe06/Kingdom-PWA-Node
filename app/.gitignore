node_modules
dist
dist-check
.wrangler
preview
preview-r*
.vite
vite.config.js.timestamp-*.mjs
*.local
*.log
.DS_Store

# The native shell (DR-0570): the Android project is generated in CI by `cap add`,
# the staged web dir is a copy of dist, and capacitor.config.json is written per
# brand by native/stage.mjs. None of the three is source.
android/
native/www/
capacitor.config.json
