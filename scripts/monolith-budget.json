{
  "file": "app/src/poe-financial-mvp-v28.jsx",
  "budget": 5932,
  "frozen": "2026-07-05 — hand-raise +3 (5929 -> 5932): add Darrell Jr to FAMILY_EMAIL_PROFILES (one allowlist entry + a two-line note on the 'family' persona scope). Onboarding a real family member, declared by Darrell; the shared allowlist constant lives in the shell and cannot leave it until the DR-0078 extraction. Pairs with migration 0080 + __tests__/family-allowlist-sync.test.js.",
  "note": "Frozen ceiling for the hybrid-modular cutover. The monolith is bug-fixes only; this line count may only go DOWN. Two deliberate hand-raises merged here (each the reviewed act this guard prescribes, reason stated in its PR): (1) +6 — the app-wide Live Worship bar is a GLOBAL surface that must mount ONCE at the shell level, above the view switch, so navigating tabs never unmounts it (logic in components/LiveWorshipBar.jsx, lib/resolve-church.js; the shell carries only import + one element + the --lwb-h offset); (2) +16 — reviewer mode ('see it as a user', Darrell 2026-07-05) must override the shell's OWN identity derivations and boot/persistence guards (isFamilyMember/isChurchStaff/isStudyCircle, data selection, hydration, save/snapshot suppression), core state plumbing that cannot leave the shell until the DR-0078 Stage-2 extraction (all heavy machinery in lib/reviewer-mode.jsx; the shell carries only the one-line wiring at each existing gate, pinned by __tests__/reviewer-mode.test.js). Prior hand-raise 2026-07-05 (+38) was the 0077 live-data rails. Re-freeze DOWNWARD with `node scripts/monolith-budget-guard.mjs --generate` at the next extraction. To RAISE this number, edit by hand with a stated reason in the PR — --generate refuses to. See docs/00-foundations/HYBRID-MODULAR-IMPLEMENTATION-PLAN.md."
}
