{
  "$schema": "https://kingdom-pwa.dev/schemas/seed-projects/v1.json",
  "seed_id": "2026-06-29-colg-compute-towers-endpoints",
  "generated_at": "2026-06-29T00:00:00Z",
  "generated_by": "church compute-towers standup session (local NAS / infra lane)",
  "instance_slug": "colg",
  "intent": "Record the TWO church tower computers beside the NovaStar processor + their three sovereign serving endpoints (Ollama LLM, XTTS-v2 voice clone, faster-whisper transcription), plus the physical/logical install and the role-separation rule, as in-app documentation under the existing 'Church Local Infrastructure' project. Exact GPU/CPU/RAM per tower are SME/UNKNOWN (Darrell to provide) -- not assumed to be any specific card. Setup runbook: docs/99-session-notes/2026-06-29-church-compute-towers-setup-runbook.md. SUPERSEDES the spec-specific 2026-06-29-colg-gpu-node-endpoints seed (which assumed 2x RTX 4070).",
  "poe_binding": "Sovereign / local / on our hardware; LAN+Tailscale only, never public. Endpoints are passive; any autonomous USE keeps the three brakes. Role separation: the live-media tower that feeds NovaStar->wall stays separate from AI compute; heavy AI off service hours (DR-0012 generalized). Verify-not-claim (DR-0076): real specs come from the on-site inventory, not assumption.",
  "apply": "Idempotent. Apply ONCE in Supabase Studio against the COLG cloud instance: run infra/seed-data/2026-06-29-colg-compute-towers-endpoints.sql (resolves the instance by slug='colg'; ON CONFLICT (instance_id, slug) DO NOTHING). Adds discussion entries to the existing project colg-local-infra-2026-06 (seeded 2026-06-23). NOT yet applied to cloud as of this commit.",
  "target_project_slug": "colg-local-infra-2026-06",
  "discussions": [
    {
      "slug": "dc-colg-towers-inventory",
      "kind": "reflection",
      "title": "Compute node = TWO tower computers beside the NovaStar (specs SME/TBD)",
      "visibility": "shared",
      "status": "open",
      "author_persona": "darrell",
      "project_slugs": ["colg-local-infra-2026-06"],
      "body": "INVENTORY (recorded 2026-06-29): the church compute node is TWO tower computers physically sitting next to the NovaStar LED-wall processor. The exact GPU / CPU / RAM / PSU in each tower is SME/UNKNOWN -- Darrell will provide them; NOT assumed to be any specific card (it is NOT 2x RTX 4070; the 3090s are not purchased). They host the serving endpoints that unlock voice + harvest + sovereign AI. THREE SOVEREIGN SERVING ENDPOINTS, one docker compose (infra/church-gpu-node/docker-compose.yml), headless + restart-on-boot + healthchecked + GPU-reserved (reservation honored when a GPU is present, ignored on a CPU-only tower): (1) Ollama :11434 - local LLMs, model SIZED TO THE TOWER'S REAL VRAM (runbook section 3 table); (2) voice-studio :8770 - XTTS-v2 few-shot voice clone, POST /speak, needs a CUDA GPU; (3) whisper-gpu :8771 - faster-whisper, POST /transcribe, the harvest no-caption fallback. The runbook section-2 inventory commands DISCOVER the real per-tower specs (GPU+VRAM, CPU, RAM, driver/CUDA, LAN + tailnet IPs); those values fill the device-inventory rows (section 6). Setup: docs/99-session-notes/2026-06-29-church-compute-towers-setup-runbook.md.",
      "links": { "dr_ref": "DR-0012" }
    },
    {
      "slug": "dc-colg-towers-install-physical",
      "kind": "decision",
      "title": "Physical/logical install of the two towers: airflow, own power, wired+fixed IPs, UPS",
      "visibility": "shared",
      "status": "open",
      "author_persona": "darrell",
      "project_slugs": ["colg-local-infra-2026-06"],
      "body": "BEST WAY: install the two towers beside the NovaStar without compromising the wall feed or the towers. PLACEMENT: do not block the NovaStar's or either tower's airflow; vented shelf/rack, OFF the floor, >=4 in intake/exhaust clearance; towers side-by-side (not stacked exhaust-into-intake); out of the cable/service path. POWER: each tower ~500-850 W under load (real PSU watts TBD) on its OWN dedicated circuit/PDU - NOT the NovaStar strip and NOT the LED-wall circuits (the wall is ~4800 W peak on its own feeds). One dedicated 20 A carries both towers (2x850 W=1700 W=14.2 A < 1920 W cap) OR one 15 A per tower; never both towers on one 15 A. UPS on the NovaStar AND the towers (protect the processor + the compute that carries state; stagger power-on, do not ride the wall's inrush). NETWORK: Cat6 to the church switch, NOT WiFi; static/reserved IPs (DHCP reservation by MAC or static); Tailscale on each tower for the home<->church mesh; record each tower's LAN IPv4 + tailnet IP. WE CHOSE own-circuit + UPS + wired-fixed-IP + vented-off-floor, NOT shared-strip and NOT WiFi, BECAUSE an AI load spike must never drop or stutter the live wall, a serving node needs a stable wired address, and the processor + compute are what must survive a blip.",
      "links": { "dr_ref": "DR-0012" }
    },
    {
      "slug": "dc-colg-towers-role-separation",
      "kind": "decision",
      "title": "Role separation: live-media tower stays separate from AI compute; AI off service hours",
      "visibility": "shared",
      "status": "open",
      "author_persona": "darrell",
      "project_slugs": ["colg-local-infra-2026-06"],
      "body": "BEST WAY: the live-media box that feeds the NovaStar -> wall stays SEPARATE from the AI-compute work. The machine rendering the wall during a service must not run model inference at the same time (they contend for GPU/CPU; a wall stutter is unacceptable). DEFAULT MAPPING (confirm once roles assigned): one tower = the live-media source into the VX1000, the other = the headless AI worker for heavy jobs (transcription backfill, batch voice synth, LLM). Towers run heavy AI jobs OFF service hours; Sunday/Wednesday service windows are a hard no for AI grind on any tower that touches the wall. If a tower MUST dual-purpose, its AI work is scheduled to NEVER overlap service times - the idle-GPU / off-hours scheduler enforces the window. WE CHOSE separate-roles + off-service-windowing, NOT one-machine-does-everything-anytime, BECAUSE live/creative production has absolute priority and AI yields (DR-0012's principle generalized from one shared card to the two-tower node).",
      "links": { "dr_ref": "DR-0012" }
    },
    {
      "slug": "dc-colg-towers-unlocks",
      "kind": "directive",
      "title": "What the towers unlock: voice clone, harvest transcription, sovereign LLM (spec-gated)",
      "visibility": "shared",
      "status": "open",
      "author_persona": "darrell",
      "project_slugs": ["colg-local-infra-2026-06", "content-engine-2026-06"],
      "body": "WHY THIS MATTERS: the two-tower node is the single dependency under three gated capabilities. (1) VOICE CLONE - XTTS-v2 on :8770 makes Darrell-hears-Darrell real (in-app recorded sample -> his own voice, same Voice-tab UI, labeled stand-in fallback). Needs a CUDA GPU on the worker tower - feasibility is SPEC-GATED. (2) HARVEST TRANSCRIPTION - faster-whisper on :8771 transcribes the rare caption-less service video locally (fast on GPU, batch on CPU), feeding the content-engine recordings->lessons spine. (3) SOVEREIGN LLM - Ollama on :11434 gives the AI tiers a local model sized to the tower's VRAM, so Talk-about / tutors / finalizer run fast + private on our hardware. NEEDS DARRELL'S EXACT SPECS to finalize: per-tower GPU+VRAM (which model fits / is voice-clone feasible), whether either tower has a CUDA GPU, PSU watts (circuit sizing), church subnet (static IPs), role assignment, CPU/RAM/disk (device inventory). Consent + faithfulness gates unchanged. WE CHOSE one-node-three-unlocks tied into the existing app seams, NOT three separate stacks, BECAUSE it is the same hardware serving the same app the same sovereign way - one place to stand up, govern, and brake.",
      "links": { "dr_ref": "DR-0076" }
    }
  ]
}
