ENGAGED. This file's PRESENCE is the streaming hold: while it exists, a live
stream is (or may be) in progress and the church CUDA towers — LEFT
(tlcmediadpt, 192.168.1.75) and RIGHT (livestream-main-pc, 192.168.1.73) — are
reserved for the stream. The idle-GPU scheduler runs NOTHING while this exists.

This is DR-0012 made a physical brake: inference never runs on a box encoding a
live stream during a service. Engaged 2026-07-14 by Darrell's directive ("dont
use the left or right cuda right now we are streaming!").

Engage any time streaming starts: create this file.
Clear only when the stream has ended: delete this file. When in doubt, leave it
engaged — a skipped batch job costs minutes; a dropped stream costs the service.
