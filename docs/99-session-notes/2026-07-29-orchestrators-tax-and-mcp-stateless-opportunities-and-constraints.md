# The orchestrator's tax and MCP 2026-07-28: opportunities and constraints

> Layer 4 working artifact. Companion to **DR-0244**, REV-0214, and LESSONS-LEARNED P36. Trigger, Darrell 2026-07-29: two hand-carried inputs — *"The Orchestrator's Tax"* (Rahul Garg, martinfowler.com, published 2026-07-28) and the MCP 2026-07-28 release note — with *"What can we get from this to use within our building processes? opportunities and constraints based on our Ways and documentation?"* and *"Lesson also..."*. Both ran the DR-0143 intake in order.

## What was brought

1. **"The Orchestrator's Tax"** — a single-incident case study: an orchestrating Claude Code session imported a background agent's full raw JSONL transcript (tens of thousands of tokens) to answer a throwaway status check, and the author's conclusion is that the scarce resource in multi-agent work is not tokens or wall-clock but **the orchestrator's working memory** — context pollution charges rent every turn after. His encoded fixes: small delegation waves; no transcript polling for status; no repo-wide git ops in concurrent agents; partition work by **cognitive locality** (shared mental model), not task count; subagents don't inherit skills — point them at files; and when encoding a lesson, **state the missing fact, not a decision procedure**.
2. **MCP 2026-07-28** — the largest protocol revision since launch. The official release post was later hand-carried whole (primary source, 2026-07-29), upgrading the earlier web-search verification: the protocol core is now **stateless** (the initialize handshake and `Mcp-Session-Id` retired — SEP-2575/2567; per-request `_meta` carrying version, identity, capabilities; optional `server/discover`), **Multi Round-Trip Requests** replace held-open streams for elicitation/sampling (SEP-2322 — `resultType: "input_required"`, client retries with `inputResponses`), routing moves to required `Mcp-Method`/`Mcp-Name` **headers** (SEP-2243), list results carry `ttlMs`/`cacheScope` **cache hints** with deterministic order (SEP-2549), **auth hardens** (RFC 9207 issuer validation; DCR formally deprecated toward CIMD; credentials issuer-bound), **Tasks** becomes a formal extension (poll-based `tasks/get`; `subscriptions/listen`), Roots/Sampling/Logging and the legacy HTTP+SSE transport are deprecated on a **twelve-month minimum window**, and all four Tier 1 SDKs — TypeScript, **Python**, Go, C# — speak it day-one. The spec's own state guidance: mint an explicit handle from a tool and let the model pass it back — visible state over transport-hidden sessions.

## The house-first trace (what the Ways already hold)

The pass ran a full architecture trace of the house's orchestration + MCP reality (file:line receipts in REV-0214's underlying map). The headline: **the article's central doctrine already lives here, in the house's own words**:

- `CLAUDE-TOOL-ROUTING.md` §5 is the same law, older: *"the orchestrator's context is the most expensive and most-reused context in the session — every token it holds is re-read on every subsequent turn"* — with the binding rule that the main Dispatch session never does substantive work itself.
- `SWIMLANES.md`: *"A session is a worker that attaches to the lanes; it is not where the state lives"* — lane state in PR labels, the conflict map, the Events spine; never in a session's memory.
- **ICM itself** (CLAUDE.md Layer 0) is context economy as architecture: the right layer at the right time, never everything always.
- The lived incidents: REV-0211's compaction-loss record (*"last time it was lost"* — machinery over memory, DR-0239), and the **Two-Session Git Race** rule (2026-05-25) — the article's git-stash-during-concurrent-writers hazard, learned here first.
- The article's "director, not doer" framing was already adopted on the 2026-07-10 pass (`director-not-doer` finding, DR-0143).

So most of this input is **confirmation, not change** — the same verdict shape as 2026-07-10. What the house had NOT named is what DR-0244 adopts.

## Opportunities (routed, with dates)

- **The four new ground rules — adopted now (DR-0244, P36).** (1) Never import a raw subagent transcript to answer a status question; (2) delegate by cognitive locality — overlapping file/domain ownership between planned agents is a consolidation signal, not a cue to spawn more; (3) skills and context don't inherit — point spawned agents at the files to load, never paste inline; (4) a new standing rule states the missing FACT, not a decision procedure. These are teachable, checkable, and now on the ledger future sessions read first (LESSONS P36).
- **A sovereign MCP server becomes architecturally cheap — when a need lands.** Statelessness removes the exact objection (session affinity, SSE long-poll) that made an MCP server a poor fit for the DR-0132 lane. A stateless MCP endpoint deploys like any FastAPI route behind Caddy on the NAS — or on the Cloudflare Pages Functions transport the app already runs. The pre-existing direction is already on record (the 2026-06-02 "Agent-Native Surface Layer" research item: every PoeTech surface gets an agent-native endpoint). The named flip-to-staged trigger: the first real need to hand agent sessions TOOLS over house state (the cloud sandbox has no LAN route — a Funnel-fronted MCP server would be the sessions' hands on the NAS), once mainstream clients speak 2026-07-28. `re-review: 2026-08-25` (DR-0244).
- **The extensions framework as the standard-not-bespoke path.** When house tools ARE served to agents, extensions give a formal way to add house semantics (bearer auth posture, audit-event emission per call — the Cage's append-only ledger pattern) without forking the protocol. Rides the same re-review.
- **Two spec patterns are the house's Ways said back.** (1) The spec's explicit-handle guidance — *"mint an explicit handle from a tool and have the model pass it back… the model can see the handle"* — is SWIMLANES' law (state in durable visible places, never hidden in a session) adopted by the protocol itself. (2) MRTR elicitation — a tool pausing to confirm before a destructive act (Supabase's own cited use: confirm before a query that would delete data) — is the house's preview-then-execute posture in protocol form. When the house serves MCP, these arrive free, already aligned. And header-based routing means Caddy — already the sovereign lane's front door — can route, rate-limit, and authorize MCP calls on `Mcp-Method`/`Mcp-Name` without parsing bodies; the Python Tier-1 SDK means the DR-0226 prioritized backend language is first-class on day one.
- **Layer 0 hygiene — the lesson pointed at ourselves.** CLAUDE.md is large, and by the article's own economics every line is a per-session tax on every future session. ICM's layering exists precisely so identity stays lean and reference lives in Layer 3. A Governor-reviewed pass asking "which Layer 0 sections are FACTS that must load first, and which are procedures that belong in a DR/foundation doc the intake already points to?" is named as an opportunity — **the Governor's call, never done unilaterally** (Layer 0 is his voice on record). `re-review: 2026-08-25` (DR-0244 §1.5).
- **Truth gap closed same-session (DR-0236):** `SWIMLANES.md` §4 had cited `scripts/orchestration/ground.sh` since 2026-06-16; no such file existed (found by this pass's architecture trace). Built to its own one-line spec — git HEAD + CI verdict + branches ahead of main, git+curl only, degrades to "unknown" never a guess — run and verified. The re-attach chain (`ground.sh` → `lanes.sh` → `conflict-map.sh`) is now real end-to-end.

## Constraints (held, with whys)

- **Recorded experience and house doctrine are senior; outside calibrations are heuristics.** The article's numeric thresholds (2–4 agents per wave; five as a consolidation signal) are one practitioner's calibration on one model and one codebase — the author says so himself, and his central cost ranking is the orchestrator grading its own mistake, unmeasured. Carried at DR-0100 tier 2: named heuristics, never law.
- **No MCP server is built today — no change just because (DR-0143/DR-0140).** The house serves NO MCP anywhere (verified: client-side consumption only — Gmail/Calendar/GitHub/Chrome MCP in sessions); every current sovereign need is met by the Supabase-bus + self-orchestrating box + FastAPI/Caddy pattern. Standing up server infrastructure ahead of a demanded workload is the demand-test inversion. Watch, with the trigger named.
- **The 2026-07-28 revision is breaking.** Old-spec and new-spec coexist through the formal twelve-month minimum window; nothing in the house's session-side MCP consumption is rewritten until the client side the house actually uses (including Claude) speaks the new revision in production. No migration work rides on this pass.
- **Subagent fan-outs stay under the existing brakes.** Cognitive-locality consolidation complements — never replaces — the three-brakes law for anything autonomous or timer-driven (DR-0225), the SWIMLANES shared-file serialization rule, and worktree isolation for genuine parallel writes. Context hygiene bounds what delegation carries; the brakes bound what delegation spends.
- **Layer 0 pruning is governance, not maintenance.** The hygiene opportunity above touches Darrell's recorded voice; it proceeds only as his reviewed decision (Tier C posture), never as an agent-side cleanup.

## Ways-review (DR-0108 questions, answered)

1. *Capability not used?* The re-attach chain's first step (`ground.sh`) was documentation-only for six weeks; now it exists and runs. And the article surfaced that the house's context-economy doctrine was scattered (tool-routing doc, swimlanes, harness habit) rather than named rules — now DR-0244/P36.
2. *Unverified "can't"?* "An MCP server doesn't fit the sovereign lane" was true under statefulness and is now false — the premise was re-verified against the new spec rather than carried stale; the honest current answer is "fits, not yet needed."
3. *Repeated friction absorbed?* Context compaction losing rules is the house's most-repeated failure class; P36 gives future sessions the named principle (subagents protect the orchestrator's memory) instead of each session rediscovering the economics.
4. *Scoped to my limits instead of the team's?* The MCP opportunity is scoped to the whole bench: it would serve ANY MCP-aware client the family runs — Claude sessions, the box agent, future sovereign models — not just the agent writing this.
5. *More streamlined way?* Fact-not-procedure rule encoding (DR-0244 §1.5) is itself the streamlining rule: it caps the bureaucracy each lesson is allowed to add.

## What shipped this pass

- `app/src/lib/research-intake.js` — two findings in the one source (`orchestrator-context-tax` adopted; `mcp-stateless-2026-07-28` watch), rendered live in Ari's record (DR-0121, no static copy).
- `app/src/__tests__/research-intake.test.js` — the pass pinned (adopted rules' language, the watch verdict, verified-date provenance, no-change-just-because); 19/19 green.
- `docs/decisions/DR-0244` + INDEX row (Next ID → 0245).
- `docs/00-foundations/_root/LESSONS-LEARNED.md` — P36 + the 2026-07-29 discovery entry.
- `docs/reviews/REVIEWS.md` — REV-0214 (Research Day pass; the cadence goes green by the record, never by promise).
- `scripts/orchestration/ground.sh` — built, run, verified (the SWIMLANES truth gap closed).
