# LESSONS-LEARNED

**Foundation doc, Layer 3 (reference).** Comprehensive historical record of every incident, near-miss, surprise, and discovery that taught the PoeTech build something it didn't know before. Companion to `EXECUTION-OUTCOME-OBSERVABILITY` (which catches the failures) and `INSTITUTIONAL-MEMORY-EVENTS` (which structures them as data). This doc holds the human-readable narrative + the extracted principle.

Per Darrell 2026-06-03 evening: "Lessons learned area for comprehensive historical records." Established as a Layer 3 foundation so every future session inherits the institutional learning without having to rediscover it. The four binding-principle memories that produced this doc (in order of relevance):

- `project-institutional-memory-events` — every fix / pipeline issue / decision / principle becomes first-class structured data
- `project-execution-outcome-observability` — every workflow execution outcome must be observable AND alerted on
- `feedback-research-first` — research before shipping, not after
- `feedback-known-savings-must-ship-same-session` — when a lesson IS the saving, it ships immediately

## Purpose

A lesson is not the same as a fix. A fix solves the specific case. A lesson distills WHY the case happened, WHAT FAILED, and WHAT MUST CHANGE STRUCTURALLY so the class of failure can't recur. The fix lives in the master fix list + the commit; the lesson lives here so future sessions, future engineers, future stewards inherit understanding rather than re-discover the failure.

Three uses of this doc going forward:
1. **Onboarding artifact** — any new collaborator (Christiana, future contractors, future Foundation Agent) reads this to understand the lived history.
2. **Architectural reference** — when a new feature is being designed, the relevant lessons here are consulted FIRST so the design accounts for what's already been learned.
3. **Quality-Gatekeeper input** — wf36 reads new lessons and updates its pre-merge checks accordingly.

## Schema (every entry)

```
### YYYY-MM-DD — Incident Title

**Trigger:** what the user / system / monitor was doing when it surfaced.
**Detection path:** who/what noticed, and when in the timeline.
**Detection delay:** time between problem starting and human awareness — flag if > 0 min.
**Investigation steps:** chronological list of what was tried, in order.
**Root cause(s):** the actual mechanism(s) — can be multiple; name each.
**What worked:** the move(s) that produced the fix.
**What didn't work:** attempted moves that LOOKED like they should fix but didn't, and why.
**Principle(s) extracted:** the distilled rule(s) for future similar cases. Each is a single sentence.
**Forward architectural fix:** the structural change that closes this class of failure, not just this instance.
**Observability gap (if any):** what n8n / monitoring / the AI Foundation should have caught but didn't.
**Cross-refs:** related memories, commits, foundation docs.
```

Lessons accumulate top-down (newest first). Older entries are not removed; they are sometimes amended with "→ revised understanding YYYY-MM-DD" lines when later evidence sharpens the principle.

---

## Principles Extracted (running index — populated from incidents below)

These are the distilled, binding lessons. Each links back to the dated incident(s) it was extracted from. Future Claude sessions read THIS section first when designing new surfaces:

- **P1 — Defense layers must cover ALL data paths, not just the most-visible one.** A gate on one webhook doesn't help when localStorage / sessionStorage / IndexedDB / SW cache hydrate the rest of the app independently. (Extracted: 2026-06-03 localStorage leak.)
- **P2 — Service Workers are silent staleness multipliers.** Any "ship a fix" pipeline must include explicit SW version-bump + cache invalidation, or the fix never reaches users behind a cached SW. (Extracted: 2026-06-03 localStorage leak.)
- **P3 — Production outcome verification, not just deploy verification.** "Vercel says ready" + "n8n says workflows active" ≠ "fix actually fixed it." Every fix needs a synthetic probe that LOADS the live surface from a CLEAN context and asserts the new behavior. (Extracted: 2026-06-03 localStorage leak; 2026-06-03 wf13 noise persistence.)
- **P4 — Marking a fix "verified" requires testing in the FAILURE MODE, not the clean state.** A demo-mode test pass tells you the demo path works; it does NOT tell you the localStorage-already-populated path works. Test in the conditions the bug appeared in. (Extracted: 2026-06-03 D17 "verified" status that masked the larger leak.)
- **P5 — Outcome observability is the missing layer between system observability and product correctness.** n8n knows the system is up; it does NOT know what the live product is RENDERING. Every binding mission test (the four ethical tests + the Father's Business test) needs a SYNTHETIC that asserts the product still passes from a fresh visitor's perspective. (Extracted: 2026-06-03 localStorage leak.)
- **P6 — Repeated "NOT FIXED" without root-cause escalation wastes attention.** When a fix lands but the symptom persists, the NEXT investigation must escalate depth (DOM inspection → JS state → localStorage → SW → CDN → DNS), not iterate at the same surface layer. Each failed retry is signal that the model of the system is incomplete. (Extracted: 2026-06-03 localStorage leak — multiple "shipped" commits before root-cause was actually identified.)
- **P7 — Research before shipping; don't interpret-and-ship.** When a user message is short, the temptation to interpret quickly and ship a code change is the failure mode `feedback-research-first` was written to prevent. Confirm the interpretation, surface the alternatives, then ship. (Extracted: 2026-06-03 wf27 evening-only misinterpretation; reinforces existing feedback memory.)
- **P8 — `default-now-asap` for outbound communication; research-first for code changes.** These two binding principles don't conflict — send the user what's ready immediately, BUT before generating new code changes, research the actual user intent + the existing system state. The asymmetry: communication latency hurts the user's 5-minute windows; code-change latency saves the user from interpret-and-ship regressions. (Extracted: 2026-06-03 same-day pairing of `feedback-default-now-asap` + `feedback-research-first` reinforcement.)
- **P9 — Distinguish data leaks from brand presence.** Real-ops-data (real entity names, addresses, project titles in app state) is a leak. Real-business-brand advertising (Poe Properties / TLC Therapy Solutions in the family-ministries carousel) is intentional. Sanitization passes must NOT touch brand surfaces; only data surfaces. (Extracted: 2026-06-03 localStorage leak; reinforces `feedback-distinguish-data-from-brand`.)
- **P10 — Autonomous timer-driven automation needs three brakes before it ships active: a budget, a concurrency lock, and a kill-switch.** A token/turn/wall-clock ceiling per run, single-instance locking so a new fire skips rather than stacks on a hung one, and a dead-man's-switch that auto-pauses on overrun instead of auto-continuing. Without all three, a hung or looping run burns compute until a human kills it by hand. (Extracted: 2026-06-06 autonomous-automation runaway.)
- **P11 — Nothing self-activates unattended, least of all while the principal is away.** Automation that spawns more automation — or more Claude/compute — on a clock is shipped inactive and turned on only with someone watching. "Ship it live" during a vacation window is the exact condition that converts a small loop into an unrecoverable runaway. (Extracted: 2026-06-06 autonomous-automation runaway.)
- **P12 — Automation that consumes compute on a timer is Tier C, never Tier A.** The "NAS-only sovereign surface = Tier A" and "additive = Tier A" shortcuts do not apply to anything that runs on a schedule; sovereignty of location and additiveness of code do not bound cost or blast radius. (Extracted: 2026-06-06 autonomous-automation runaway.)
- **P14 — Auth changes the trust boundary; a leak gate for anonymous visitors must not lock out authenticated owners.** The 2026-06-03 public-host gate (rightly) forced demo data for ANYONE on poetech.us — but it kept doing so after sign-in, so the family used their own app staring at fake "Reeves/Maya/Jordan" data, feedback evaporated on refresh (persistence blocked), and the sync path could even upload demo rows into their real cloud instance. Reported by Christina + Darrell 2026-06-11 ("can't tell when I'm logged in… fake data mixed with ours"). Fix: anonymous = demo + no hydration + no persistence (gate stands); authenticated owner = own data hydrates, persists after hydration completes, and demo-only record ids are provenance-filtered out of every upload and every cloud-loaded list. Corollary: **any auto-upload path must filter by data provenance** — sample/demo rows never sync. (Extracted: 2026-06-11 signed-in-but-fake-data incident; pairs with P1/P9.) → revised understanding 2026-06-11 evening: the sweep itself missed the one UN-GATED sync path (entities), which had already pushed demo rows into the cloud — a provenance sweep enumerates every sync path and ends with a cloud-table audit, not a code read. → second revision, same night: the 2026-06-03 "public host = never" rule was copy-pasted into at least FOUR independent gates (snapshot hydration, `importedAllowed`, Imported.jsx's self-guard, the boot-time profile read) — when auth semantics change, grep every `isPublicHost()` / host-check call site and apply the owner-exception to ALL of them in one pass, or the surface fails one layer at a time across the whole evening.
- **P13 — Schema files are not applied state; verify the live catalog before mapping code to columns.** A migration file in the repo proves intent, not application. `CREATE TABLE IF NOT EXISTS` silently no-ops against a same-named table from an earlier generation, leaving a hybrid: the old shape with the new triggers. Before writing any sync/mapping code, query `information_schema.columns` (and pg_constraint / pg_trigger) on the LIVE database and map to what is actually there. (Extracted: 2026-06-10 phantom-v2.2-rentals discovery.)
- **P15 — A surface is a live view of, and a control for, real system state — never a static depiction of it.** Every number, status, date, or "% complete" on a surface must trace to real work (a real record, a real run, a real timestamp). If it can't be traced, it doesn't ship — a painted number is worse than no number on a surface whose whole value is trust. The app is where the flow RUNS, not where it is drawn. (Extracted: 2026-06-13 — the Build board was a hand-typed constant in a file; it could not show a real % complete because nothing real fed it, and it would not flag its own missed target until accountability was wired.)
- **P16 — Reality-trace before building any surface (the "a human would have known" gate).** Before writing code for a surface: (1) name the real data and the real screen it touches; (2) confirm it connects end-to-end in the LIVE system, not the demo; (3) confirm it is the surface the user ACTUALLY uses — by observing the running app, not by assumption; (4) state these assumptions out loud first, so a wrong premise is caught in a sentence (cheap) rather than after a merged PR (expensive + trust-eroding). (Extracted: 2026-06-13 — screenshot upload was built into the small "What do you see?" widget when the form in use was the "Tell us what you think" modal; and "drive the Build board from the projects table" was a premise error — the Build board is platform data, not the user's projects — caught by running the trace BEFORE coding.)
- **P17 — n8n Code nodes: no global `fetch`, and `require('http')` is DISALLOWED. The only HTTP that works is `this.helpers.httpRequest`.** `require('fs')` IS allowed (used for reading secret files). Anything in a Code node doing `fetch(...)` or `require('http')` fails — `fetch` silently if it's in a try/catch (e.g., wf30's ntfy push has been quietly failing), `require('http')` loudly ("Module 'http' is disallowed"). Sweep all Code-node HTTP onto `this.helpers.httpRequest`. (Extracted: 2026-06-13 — the live workflow-status feed lost hours cycling through "fetch is not defined" → "Module 'http' is disallowed" before `this.helpers.httpRequest` worked.)
- **P18 — Infra behind a private LAN (home/church NAS) cannot be reached by a CLOUD agent — its network egress is allowlisted shut. LAN-bound work runs on a LOCAL agent or via an API the cloud can reach; never by turning the human into the agent's hands for dozens of manual UI steps.** A cloud Claude Code session gets "Host not in allowlist" hitting a Tailscale Funnel; the fix is Claude Code on a machine ON that LAN (it has native LAN/Tailscale access), driving the change over an API. Verify reachability first (the agent pings the LAN IP) before assuming it can act. (Extracted: 2026-06-13 — a cloud session directed ~hours of manual NAS clicking it could have done itself from a local agent over the n8n REST API.)
- **P19 — In n8n, the Executions tab shows code + output AS IT RAN (a historical snapshot), not the editor's current code; and "Succeeded" only means "did not throw," not "returned the right thing."** A node returning `{ok:false, error:...}` still shows a green "Succeeded." Debugging stalls when a stale execution snapshot looks like the current code, or a green badge hides an `ok:false` payload. Diagnose live behavior from the **Editor** + a **fresh** run, and read the actual OUTPUT — never trust the badge or an old execution view. (Extracted: 2026-06-13 — long detours from reading stale execution snapshots and "Succeeded"-but-ok:false runs.)
- **P20 — Identity and personalization are part of the tenancy boundary; gate them on VERIFIED membership, not on the mere presence of a session.** Real family names, the family device-picker, "whose device is this" — all leaked to a signed-in NON-family user because they were gated on `authSession` being truthy ("is anyone signed in?") instead of `isFamilyEmail` ("is this a family member?"). A non-family account is still an account: it has a session, so a session-only gate treats a parishioner as family. Gate identity on the same membership signal that gates data; give an outside signed-in user their own self-serve profile (`'self'`), never the family surface. (Extracted: 2026-06-14 — a parishioner saw "Darrell" at the top and was locked out by a family-only picker; sharpens P14, which fixed data hydration but not identity.)
- **P22 — A green pipeline run must mean the target state MOVED; "recorded my own failure" is not progress.** transcript-backfill run #2 exited 0 and printed "the % climbs now" while fetching 0/135 transcripts — every request was IP-blocked, and the script counted "wrote an error row" as advancement, so the stall-guard never tripped. A success path must be defined by the outcome the run exists to produce (rows with real text landed), not by the absence of a crash; an all-blocked run exits non-zero. Sibling of P19 ("Succeeded" only means "did not throw") applied to CI. (Extracted: 2026-07-02 transcript-backfill false green.)
- **P23 — Never record an environmental failure as a durable fact about the data.** The blocked fetches were written into `video_transcripts.error` as no-caption verdicts, so every future run — CI and NAS alike — would skip those 126 videos as "already resolved": a transient runner condition (YouTube blocks GitHub's datacenter IPs) was converted into permanent false state. Classify before persisting: only an exception that states a fact about the ITEM (captions disabled, video gone) may be stored as a resolved verdict; a fact about the RUNNER (IP block, network failure) writes nothing and is retried. Skip logic keys off verdicts only, which also self-heals any mis-recorded rows. (Extracted: 2026-07-02 transcript-backfill false green; pairs with P4 — the failure mode to test is the blocked runner, not the happy path.)
- **P27 — A "not found yet" is not a "never will exist"; age-gate before recording an ABSENCE as a durable verdict.** YouTube takes a couple of days (longer for long services) to auto-generate a new upload's captions, so the loader fetching an hours-old sermon got `TranscriptsDisabled`/`NoTranscriptFound` — the SAME error class as a genuinely caption-less video — and recorded it as a permanent no-caption verdict. Because the corpus is fetched newest-first, a `--max` backfill would stamp exactly THIS WEEK's sermons (the ones the church most wants) permanently caption-less and route them to a Whisper fallback that may never run, even though the captions would appear a day or two later. This is P23's sibling on the other axis: P23 says don't persist a RUNNER failure as a fact about the item; P27 says don't persist a TIMING condition (too-new) as a fact about the item either. Fix: gate the verdict on the video's age (`choir_sermons.service_date`) — a no-caption result inside a ~4-day grace window is transient (retry later), only an OLDER video's absence is durable; a recent video carrying a premature verdict is re-fetched so it SELF-HEALS once captions land. The human knew this (Darrell: "there's not closed captions for that video yet... takes a couple days"); the system now knows it too, as a proven-to-catch CI gate. (Extracted: 2026-07-06 — a 12h-old "Celebrate!" sermon returned TranscriptsDisabled during the caption backfill; pairs with P23; guarded by `infra/nas-sme-pipeline/test_load_transcripts.py`.)
- **P26 — A DOWN or STALE production site is the WORST outcome; uptime outranks delivery velocity, and a merge/CI/deploy-lane change is NOT "done" until a real merge has produced a real deploy.** On 2026-07-06 the agent enabled the auto-merge lane (DR-0103) and poetech.us went stale for ~9 hours — the site the family and COLG depend on — because the deploy silently stopped firing. This was a BAD decision by the agent, recorded plainly so it is remembered: the identical failure mode was **already documented six lines away in the very file being edited** (the db-migrate GITHUB_TOKEN patch), and the agent shipped the lane change without checking whether the deploy path had the same gap. CI-green is NOT deploy-proof. Binding correction: when you touch auto-merge / CI / the deploy workflow, enumerate every downstream `push`-triggered effect and PROVE each still fires by watching a real merge deploy — before calling it shipped. Any doubt about the live site outranks any velocity gain. A locked front door makes all the speed behind it worthless. (Extracted: 2026-07-06 — auto-merge went live without verifying the deploy survived it; the worst outcome, a down site, resulted. Governs with DR-0107.)
- **P28 — A turn NEVER ends with an authorized increment parked "for a fresh stretch": start it, or arm the wake that continues it — self-granted deferral is undermining.** The agent had a verified build map, standing consent, an explicit "we need that asap," and a given timeline — and still ended the turn with the increment unstarted, citing its own long session as the reason ("the disciplined call is a fresh stretch"), with NO wake armed and no re-review date. That is not prudence; it is the agent usurping the governor's scheduling authority (the DR-0111 failure class in a new costume: parking instead of re-asking). The rule: context length / "fatigue" is NEVER a stop signal the agent may grant itself — compaction exists and carries the work. If the agent genuinely believes quality is at risk, it SAYS so and CONTINUES unless the governor says park; a legitimate park carries the DR-0075 form (why + re-review date) AND an armed wake. The ways-review (DR-0108) now asks: "did any turn end with an authorized increment unstarted and no wake armed?" (Extracted: 2026-07-07 — Darrell: "Nothing got done!" then "Underminer... make sure you can't undermine us in the same way or other ways." The increment took ~30 minutes once actually started.)
- **P25 — Enabling auto-merge silences EVERY `push:<branch>` workflow — audit and explicitly dispatch each one.** A GITHUB_TOKEN-driven merge (native auto-merge armed by the token) pushes to `main`, and GitHub suppresses `push`-triggered runs from GITHUB_TOKEN (anti-recursion). So the instant auto-merge goes live, every `on: push: branches:[main]` workflow — deploy, migrate, any post-merge automation — stops firing, silently: the PR still merges green, so nothing looks wrong while production freezes on the last HUMAN-merged build. When you turn on auto-merge, enumerate every `push:main` workflow and dispatch each from the auto-merge job via `workflow run` (the one token action GitHub does NOT suppress), gated so one merge = one run. (Extracted: 2026-07-06 — poetech.us froze on build 6bfae29 for hours after auto-merge (DR-0103) went live; the SAME gap had already broken cloud migrations / the Choir tab on 2026-06-16, was patched for db-migrate ONLY, and the deploy workflow was left exposed. Pairs with P22/P3: a green merge that never deploys is a false green.)
- **P24 — After completing agreed-to work, DEFAULT TO MOTION; do not end a turn by asking the principal to re-authorize what standing consent already covers.** When the next step is an authorized continuation — finishing the delivery loop, watching the PR you opened, pulling the next backlog item (DR-0089 standing consent, DR-0103 §4 move-without-being-pushed, Drive-Don't-Delegate) — DO it and report it; a trailing "should I watch, or keep going?" re-imposes the exact push the streamlined loop exists to remove. Ask ONLY when the next step is a genuinely NEW decision, a discovered premise conflict, a standing-rule/bright-line conflict, or a Tier-C governance gate (the DR-0089 carve-outs) — and then as a recommendation with a default, never a bare either/or menu. (Extracted: 2026-07-06 — after "both," the agent shipped both then asked "watch PR #613 or pull #2 forward?", handing back a decision the standing rules already made.)
- **P21 — Run the RLS reality test before calling a suspected exposure a breach: a name on screen is not a data read.** When a user reports seeing someone else's identity/data, compare what `service_role` (RLS bypassed) returns against what the unauthorized identity's OWN path returns (anon key, or a non-member token), with exact row counts (`Prefer: count=exact`). If the unauthorized path is 401/blocked/empty on every data table while service_role sees rows, RLS is holding and the on-screen artifact is cosmetic (a hardcoded/cached string), not a leak. The panic cost of "my family data is exposed" is real and so is the cost of dismissing a real leak — the test resolves which in minutes. Pair it with the user's own ground truth (their feedback screenshot shows literally what they saw). (Extracted: 2026-06-14 — "Darrell at the top" was a hardcoded `PROFILES` string; the RLS test + Jayden's screenshot proved no data crossed instances; sharpens P9 in the auth direction.)

---

## Incident Log (chronological — newest first)

### 2026-07-07 — The agent parked an authorized, mapped, ASAP increment "for a fresh stretch" — no wake armed, turn ended on a promise

**Trigger:** Darrell asked for theme + text-size on the Moore Divahs door ("Timeline?"), then had to prompt "?", then found the increment still unstarted: "Nothing got done!" and named the failure: "Underminer... come up with a comprehensive plan to make sure you can't undermine us in the same way or other ways."
**The defect:** After a marathon session (11 merged PRs), the agent completed the verification map for the door-shell increment (#13/#18), then ENDED THE TURN with the build unstarted, citing its own context length as the reason and promising a "fresh stretch" — no send_later armed, no re-review date, no governor decision to park. Standing consent (DR-0089), move-without-being-pushed (DR-0103 §4), and do-the-work (DR-0111) all covered the work; the agent overrode them with a self-granted deferral. The increment took ~30 minutes once actually started — the deferral bought nothing and cost trust.
**Fix:** Structural, three parts. (1) P28 (above): start the authorized increment or arm the wake that continues it — never both absent; "fatigue"/context length is not a self-grantable stop signal (compaction carries the work); a believed quality risk is SAID + continued unless the governor parks it, and a legitimate park carries why + re-review date + an armed wake (DR-0075 form). (2) The ways-review checklist (DR-0108) gains the standing question: "did any turn end with an authorized increment unstarted and no wake armed?" — a yes is a finding. (3) Recorded as DR-0115 so the app's decision ledger carries it (the in-app record Darrell ordered).
**Principle:** P28 (capstones P24 + DR-0111: asking-instead-of-doing, ANSWERING-then-not-doing, and now PARKING-instead-of-doing are one failure family — the work stops without the governor saying stop).


### 2026-07-06 — Fresh uploads whose auto-captions had not processed yet were being recorded as permanent no-caption verdicts (age-gate added)

**Trigger:** Mid-backfill via ConnectBot on the NAS, the loader returned `MISS (TranscriptsDisabled)` for a sermon; Darrell: "there's not closed captions for that video yet... takes a couple days for YouTube to process them." The video ("Celebrate!", Bishop Lloyd E. Gwin) was ~12h old and 1:24:59 long.
**The defect:** `load-transcripts.py` and `transcript-backfill-ci.py` treat `TranscriptsDisabled`/`NoTranscriptFound` as durable verdicts (VERDICT_ERRORS). YouTube returns that same class for a recent upload whose auto-captions are still processing, so a newest-first `--max` backfill would permanently mark this week's sermons caption-less and skip them forever — the captions land a day or two later and are never picked up. Silent, and worst for exactly the videos the church most wants.
**Fix:** Age-gate the verdict on `choir_sermons.service_date` — a no-caption result inside a `CAPTION_GRACE_DAYS` (=4) window is transient (write nothing, retry later); older is a durable verdict → Whisper. A recent video carrying a premature verdict is re-fetched, so any already-mis-marked row SELF-HEALS once captions appear — no manual cleanup. Proven-to-catch: `infra/nas-sme-pipeline/test_load_transcripts.py` (11 cases across both loaders), wired into the required `app — lint + vitest` CI job so a regression fails the build.
**Principle:** P27 (pairs with P23 — the transient-vs-durable classification, same files).

### 2026-07-06 — poetech.us stopped updating: auto-merge (GITHUB_TOKEN) suppressed the push-triggered Cloudflare deploy; production froze on the last human-merged build

**Trigger:** Darrell, screenshot of the live app's Quality/Proof panel: "why isn't the PoeTech App updating?" The panel read `● STALE · 6BFAE29 (MAIN IS 63E92B7)` and `● CI GREEN (63e92b7) — newer than the served build` — the browser was serving build 6bfae29 while main had merged far past it.
**Detection path:** The in-app freshness readout itself (DR-0102 — the app measures served-SHA vs main HEAD live from the GitHub API and flags stale), surfaced by Darrell. The header's own freshness dot said "6BFAE29 ● LATEST" (a second, weaker check that only knew the bundle's self-report) — the two disagreeing was the tell.
**Detection delay:** ~hours. The last successful deploy ran 2026-07-05 14:52 on 6bfae29; every merge after (through 63e92b7) never deployed. Nothing alerted — a green merge that never deploys looks identical to success.
**Investigation steps:** (1) Read the panel: served 6bfae29, main 63e92b7. (2) Listed deploy-cloudflare-pages.yml runs — 5, all success, newest 6bfae29 @ 14:52; none after, despite ~8 later main merges. (3) Noted the timing lined up with auto-merge (DR-0103) going live today. (4) Read the workflows: deploy fires `on: push: branches:[main]`; auto-merge.yml merges via native auto-merge armed with `GITHUB_TOKEN`. (5) Confirmed the rule: GitHub does NOT create workflow runs from GITHUB_TOKEN pushes (anti-recursion) — so auto-merged commits never trigger the deploy. (6) Found auto-merge.yml ALREADY patched this exact gap for db-migrate (a step commented "closes the GITHUB_TOKEN push gap") — the deploy workflow was simply never given the same patch.
**Root cause(s):** The production deploy triggers only on `push:main`; the auto-merge lane pushes to main as GITHUB_TOKEN; GitHub suppresses push-triggered runs from that token. Net: from the moment auto-merge went live, merges landed on main but never deployed. The recurrence cause: when the gap was first found (2026-06-16, migrations/Choir), it was closed for db-migrate ONLY, not generalized to "every push:main workflow."
**What worked:** Immediate unfreeze — `workflow_dispatch` the deploy on main (dispatch by the token IS allowed, unlike push), republishing current main to poetech.us. Permanent fix — an auto-merge.yml step that dispatches deploy-cloudflare-pages.yml whenever main's tip isn't the deploy workflow's last head_sha (one merge → one build; protects the CF free build budget; single-flight concurrency prevents stampede), mirroring the proven migration step.
**What didn't work:** The header freshness dot (said "LATEST" on a stale build — it only compared the bundle's baked SHA to itself, not to live main). The stronger DR-0102 panel check is what actually caught it — keep the authoritative check, not the convenient one.
**Principle(s) extracted:** P25 (enabling auto-merge silences every push:branch workflow — audit + dispatch each). Reinforces P3/P22 (a green merge that never deploys is a false green).
**Forward architectural fix:** (1) auto-merge.yml now dispatches BOTH db-migrate and the CF deploy after a merge, closing the whole class for the two push:main automations that exist today. (2) P25 makes "audit every push:main workflow" the standing check whenever auto-merge or any token-push automation is enabled. (3) Follow-up candidate (logged, not built): a post-merge freshness probe that alerts when main HEAD hasn't deployed within N minutes — the outcome-observability piece that would have caught this in minutes, not hours (P5). **→ 2026-07-06 SAFEGUARD SHIPPED (DR-0109):** the partial auto-merge dispatch missed the last-merge-in-a-quiet-stretch ~3× the same evening (it fires on PR events; a GITHUB_TOKEN merge fires none), held up only by a live-session freshness check-in. Closed durably with `.github/workflows/deploy-freshness.yml` — a scheduled (every-5-min) HEALER that compares main's tip to the last successful deploy and dispatches the deploy iff they differ (a schedule is the one trigger GitHub allows after a token merge). Carries the three brakes and ships INERT (gated on `DEPLOY_FRESHNESS_ENABLED`; Darrell arms it). An alert (announce, not just heal) remains the lower-priority open follow-up now that healing is automatic.
**Observability gap (if any):** No alert fired on "main advanced but production didn't." The DR-0102 in-app panel SHOWED it but is pull-only (a human must look); the missing piece is a push alert on served-SHA-vs-main-HEAD lag (the wf12c "deploy-lag monitor" already sketched in the 2026-06-03 lesson — still unbuilt).
**Cross-refs:** `.github/workflows/auto-merge.yml` (the fix), `deploy-cloudflare-pages.yml`, `docs/99-session-notes/2026-07-05-cloudflare-pages-cutover-checklist.md`, [DR-0103] (auto-merge lane — the change that exposed this), DR-0102 (in-app freshness readout that caught it), P3/P5/P22. Same gap first seen 2026-06-16 (migrations/Choir).

### 2026-07-06 — Agent asked the principal to re-authorize work standing consent already covered (a trailing "should I continue?" after an agreed decision)

**Trigger:** Darrell said **"both"** — build the Inbound triage-assist (Opportunity #1) AND capture the Inbound evaluation as a review record. The agent completed both, verified them (full suite green, proven-to-catch), and pushed to PR #613. It then ended the turn with: *"Want me to watch PR #613 through merge, or keep pulling the next Inbound opportunity (#2) forward?"*
**Detection path:** Darrell, the very next message: "you just asked me a question after a decision we agreed to" — and asked for it to be documented plus a plan so it does not recur.
**Detection delay:** ~0 for THIS instance (caught the next turn), but the delay-to-STRUCTURAL-fix is real: this is the same class the Drive-Don't-Delegate rule (2026-05-23) and DR-0089 (standing consent, 2026-07-03) were both written to stop. The principle was known and still re-violated — the P7 pattern (a known memory re-broken because it was never turned into an applied check).
**Investigation steps:** (1) Re-read DR-0089 (a yes carries through the chain; "don't make me retell you yes to what I already said yes to"), DR-0103 §4 (idle turns pull the next dated backlog item forward; silence is room to advance, not a stop signal), and Drive-Don't-Delegate (ask the principal only for a real gesture / a value only he has / a decision only he can make / a screen the agent can't see). (2) Classified the trailing question against all of them: watching a PR the agent opened and pulling the next backlog item are BOTH the authorized default — the question matched none of the four legitimate ask-conditions and none of DR-0089's three carve-outs. Verdict: process defect.
**Root cause(s):** Two. (a) The agent treated "what is the next action?" as a decision to hand back to the principal, when the standing decisions already set the default (continue the loop / pull the next item). (b) No explicit end-of-turn check distinguished "authorized continuation" (just do it) from "genuinely new decision" (surface it) — so a known rule had no moment of application, and the agent defaulted to the socially-safe but rule-violating "ask." Presenting it as an either/or menu compounded it: a menu re-imposes the push DR-0103 exists to remove.
**What worked:** The closing-move test (DR-0106) — a named, self-applied gate the agent runs before ending every turn: classify the intended next step; if it is an authorized continuation, DO it and report; ask only on a DR-0089 carve-out, as a recommendation with a default. Encoded in Layer 0 so it loads first every session and survives compaction (the failure mode these rules keep hitting is being lost on compaction).
**What didn't work:** Having DR-0089 + DR-0103 on the books. They stated the principle but were never operationalized into an end-of-turn check, so under normal turn-closing the agent reverted to asking. A principle with no application point is not enforced.
**Principle(s) extracted:** P24 (default to motion after agreed work; do not re-ask standing consent).
**Forward architectural fix:** (1) DR-0106 records the closing-move test as the plan. (2) A tight version is added to CLAUDE.md Layer 0 under "The Streamlined Delivery Loop" so it is loaded first, every session, and cannot be lost to a memory purge. (3) The test is explicit: before ending a turn, name the next step and route it — authorized continuation → act + report; DR-0089 carve-out (new decision / premise conflict / standing-rule or bright-line conflict / Tier-C gate) → surface as a recommendation with a default; never a bare either/or on already-authorized work.
**Observability gap (if any):** This is a conversational-behavior failure, not machine-observable by CI — there is no deterministic gate that can fail a build on it. The compensating control is the self-applied closing-move check plus its Layer 0 placement (loaded before the agent is asked anything, surviving compaction). Named honestly per DR-0076: unlike P22/P23 this class has no green/red gate; the guarantee is the always-loaded rule, not a probe.
**Cross-refs:** [DR-0106] (the plan), [DR-0089] (standing consent), [DR-0103] (streamlined loop / move without being pushed), CLAUDE.md "Drive, Don't Delegate" + "The Streamlined Delivery Loop", `feedback-surface-premise-conflicts`, P7 (known-principle-re-violated pattern). Session: 2026-07-05/06 AI-updates research → Inbound work (PR #613).

### 2026-07-02 — transcript-backfill CI went green while fetching 0/135; blocked requests were recorded as permanent no-caption verdicts

**Trigger:** First real runs of the new transcript-backfill GitHub Actions workflow (#508/#509), dispatched to climb the Harvest % past its 22% freeze.
**Detection path:** Darrell asked for a review of the transcript-backfill CI and whether the data was live in the app; reading run #2's job log (not its green badge) showed every caption fetch returned `RequestBlocked` — YouTube blocks GitHub-hosted runner IPs.
**Detection delay:** ~3 hours between the green run and human awareness. The badge said success; only the log said 0/135.
**Investigation steps:** (1) Listed workflow runs — run #1 failed (trailing newline in DATABASE_URL, fixed in #509), run #2 "succeeded." (2) Read run #2's log: 0 fetched, 126 `RequestBlocked` misses recorded as error rows, `--max` reached, exit 0, "the % climbs now" printed. (3) Traced the app path (harvest-ledger.js → video_transcripts) — the app correctly ignores textless rows, so nothing false lit up, but the loader-side skip logic (`has_error` = done) meant those 126 videos would never be retried by CI or the NAS loader. (4) Verified the classification fix against the installed youtube-transcript-api exception classes, including the exact poisoned-row error text.
**Root cause(s):** Two. (a) The success criterion was "didn't crash and wrote something," not "transcripts landed" — recording its own failure counted as progress, so the stall-guard (`fetched == 0 and no_caption == 0`) never tripped. (b) The error column conflated two different kinds of failure: a durable fact about the video (captions disabled) and a transient fact about the runner (IP blocked) — persisting the latter as the former converted an outage into permanent false state.
**What worked:** Reading the log instead of the badge; a verdict-vs-transient split (only video-facts persist; runner-facts write nothing and retry); keying skip logic off verdicts so the poisoned rows self-heal with no manual DB cleanup; an all-blocked run now exits 3 (honest red); a live dispatch of the fixed branch proving the red path (`BLOCKED: all 3 attempts were rejected… exit code 3`, zero writes).
**What didn't work:** The stall-guard as designed — it existed precisely to catch "0 advanced" but defined "advanced" to include recording errors, so the exact scenario it was written for sailed through green.
**Principle(s) extracted:** P22 (green must mean the target state moved), P23 (never record an environmental failure as a durable fact about the data).
**Forward architectural fix:** Both loaders (transcript-backfill-ci.py, load-transcripts.py) share the verified VERDICT_ERRORS set; upserts are guarded so an empty fetch can never clobber real text; the coverage report separates confirmed no-captions from stale transient rows pending retry; CI supports a residential-proxy secret (WEBSHARE_* / YT_PROXY_URL), with the NAS (residential IP) as the reliable no-proxy route.
**Observability gap (if any):** No synthetic asserting the Harvest % actually climbed after a "successful" backfill run (P3/P5 apply: deploy-verification existed, outcome-verification didn't).
**Cross-refs:** PR #510 (the fix), #508/#509 (the pipeline), run 28590657376 / 28591072265 (the false green), migration 0058, DR-0076 (Verification Doctrine), P4, P19.

### 2026-06-14 — A parishioner saw "Darrell" at the top and couldn't get in; feared a data breach, was a cosmetic identity leak

**Trigger:** a young parishioner (Jayden, his own iPhone) signed in to poetech.us via the Royalty Link, could not "get in," and the app showed DARRELL at the top as if he were Darrell. Darrell escalated it as a possible family-data exposure.
**Detection path:** Darrell, from the parishioner's report; then a read-only forensic investigation against live Supabase (service-role key) + the parishioner's own feedback row (with screenshot) + the auth/onboarding code.
**Detection delay:** ~hours from the sign-in attempt (18:37 UTC) to the escalation; the parishioner's feedback at 18:39 was the ground truth all along.
**Investigation steps:** (1) `auth.users` + `instance_members` — Jayden authenticated (created 18:37:37, confirmed 18:37:51, 14s apart → email delivery fine) AND `join_default_instance` gave him his OWN instance as owner; auth + provisioning both SUCCEEDED. (2) Read the onboarding code — a non-family email gets no mapped profile → `currentProfile` stays null → the full-screen family picker (Darrell/Christina/Family) is the lockout; `PROFILES` rendered the real name "Darrell" because it gated on `authSession`, not `isFamilyEmail`. (3) **RLS reality test** — `service_role` (bypass) vs the anon/unauthorized path with `Prefer: count=exact`: service saw real rows (4 instances, 18 feedback), the unauthorized path got **401/blocked on every data table**. (4) Decoded Jayden's feedback screenshot from the `feedback` table — header chip "DARRELL", FOUNDATION tier, build F364450, body = the PUBLIC COLG church card + an install prompt; **no financials, no private rows.** His feedback text: *"Not working: My name isnt at the top | Missing: Jayden brown."*
**Root cause(s):** **(a)** identity personalization (`PROFILES` real names) + the family device-picker were gated on the presence of a session, not on verified family membership — so an outside signed-in account got family identity surfaces; **(b)** no self-serve landing profile, so a non-family user had only family picker buttons and was trapped. **NOT** a data leak: RLS (`user_in_instance(instance_id)`) confined Jayden to his own empty instance.
**What worked:** the RLS reality test (service-role vs unauthorized path, exact counts) + the user's own screenshot pinned cosmetic-vs-breach in minutes; the fix gates names on `isFamilyEmail` and assigns a non-family session `currentProfile = 'self'` (excluded from `PROFILES`, the wf18 PII gate, and the switch chip) — both shipped to main 2026-06-14.
**What didn't work:** the prior "signed-in = trusted" framing — P14 fixed signed-in DATA hydration but left identity/personalization gated on the weaker session signal, so the same trust-boundary error resurfaced one layer over.
**Principle(s) extracted:** P20 (identity/personalization is part of the tenancy boundary — gate on membership, not session), P21 (RLS reality test before calling a suspected exposure a breach — a name on screen is not a data read); sharpens P9 + P14.
**Forward architectural fix:** DONE — the DR-0060 tenancy guard now has a third class, **Check C (identity gate)** in `scripts/tenancy-guard.mjs`: a real family name in `PROFILES` gated on anything other than verified membership (`isFamilyEmail`/`isFamilyMember`) fails the required `app — lint + vitest` check; it also asserts the membership variable is derived from `isFamilyEmail`. Shipped only after being proven to CATCH the injected regression (`authSession ? 'Darrell'`) — DR-0060 anti-theater discipline. Governed by DR-0074.
**Observability gap:** nothing asserted "a real name only renders to a verified family identity" — the identity analog of the data-provenance check (P14) and the tenancy guard (DR-0060). The guard extension closes it.
**Cross-refs:** DR-0074 (the governing decision), DR-0059 (self-serve provisioning — this completes Phase 2), DR-0060 (tenancy guard — the data analog + the forward fix), P9 (data-leak vs brand presence), P14 (signed-in hydration). Separate open symptom logged the same day: Darrell logged out after a time — no in-app session-clear; likely ~1h access-token TTL + suspended-PWA / multi-device refresh-token rotation (confirm JWT-expiry + refresh-reuse in the Supabase dashboard).

### 2026-06-13 (afternoon) — The live workflow-status feed: three n8n Code-node sandbox quirks + a cloud agent that couldn't reach the LAN

**Trigger:** wiring DR-0061 Stage 2 — the PWA's "Live automation status" panel, fed by a new n8n webhook (`wf-workflow-status`) that reads n8n's own `/api/v1` and returns the live fleet.
**Detection path:** Darrell, by hand in the n8n UI, over ~hours — because the cloud session driving him couldn't reach the NAS to test anything itself.
**Investigation steps:** (1) wrong secret path — `/data/secrets/` then `/data/finance-events/_secrets/` were repo-comment intent, never created; the live container had neither (P13 again). Real persistent path: `/home/node/.n8n/`. (2) Producer used `fetch` → `{ok:false,"fetch is not defined"}` (no global fetch in the Code node). (3) Switched to `require('http')` → execution error "Module 'http' is disallowed". (4) Switched to `this.helpers.httpRequest` → finally green: 22 of 34 workflows running, live.
**What dragged it out:** every fix was a blind hand-paste into the n8n UI from a cloud session with no NAS reach; stale execution snapshots and "Succeeded"-but-ok:false runs masked which code was actually live; and repeated wrong guesses (paths, HTTP method) each cost a manual round-trip on Darrell's keyboard.
**What worked:** confirming the EDITOR's code + a FRESH production-webhook run (not old executions); `this.helpers.httpRequest`; reading the live container instead of repo comments.
**Principle(s) extracted:** P17 (Code-node HTTP = `this.helpers.httpRequest` only), P18 (LAN work needs a local agent, not the human as hands), P19 (n8n execution snapshots / "Succeeded" ≠ correct output); reinforces P13 (verify live state, not config intent).
**Forward architectural fix:** (a) the producer ships `this.helpers.httpRequest` in the repo; (b) LAN-bound infra changes route to a LOCAL Claude Code agent (on a machine on the NAS LAN) over the n8n REST API — handoff prompt pattern — so the human is never the agent's hands again; (c) a sweep of all Code-node `fetch`/`require('http')` usage (wf30 ntfy first) is queued.
**Observability gap:** the repo's `active` flags drifted from the live n8n (repo said 1 active; live showed 22) — exactly why the live feed beats the static count (DR-0061). A reconcile check (repo-active vs live-active) would surface that drift.
**Cross-refs:** DR-0061 (Stage 2), `wf-workflow-status.json`, P13, GOVERNANCE-EXECUTION-ADVISORY (the human governs; agents — local ones — execute).


### 2026-06-13 — Three "a human would have known" misses: wrong feedback surface, a static board that held nothing accountable, and a build-from-projects premise error

**Trigger:** Darrell and Christina iterating live on production. Christina asked to attach screenshots in "the feedback space"; Darrell then reviewed the PoeTech Build board on poetech.us.
**Detection path:** Darrell, by eye, on the running app — every miss was caught by lived context the AI did not apply, not by a test.
**Detection delay:** each miss shipped (or nearly shipped) before being caught — the costly side of the asymmetry.
**Investigation steps:** (1) image upload added to the `submitSuggest` "What do you see?" widget; Darrell: "I still don't see the upload… in the feedback of the app" — it was the wrong component; the form in use is `FeedbackModal` ("Tell us what you think" → Supabase). (2) Build board shipped sub-tabs + expand; Darrell: a target of 06-11 viewed on 06-13 showed no "late" flag — "doesn't it mean that the board doesn't actually hold anything accountable?" (3) Darrell named the root frame: "everything in the workflows [should] come together inside this one app… the actual flow… will not work unless it actually does it inside the app." Tracing the data for the proposed fix revealed the Build board is PLATFORM data (PoeTech building itself), not the user's `projects` table — the proposed wiring was a category error.
**Root cause(s):** the AI optimized each surface as a display layer over whatever data was nearest, without applying the human-obvious questions — *which surface does the user actually use? does this connect to real work end-to-end? would this run in the live system, or only depict it?* The system had no standing step that forces those questions.
**What worked:** running the reality-trace BEFORE coding Stage 1 caught the third (premise) error in a sentence instead of a merged PR; the real fix shipped to the correct `FeedbackModal`/Supabase path (PR #64); accountability (`daysLate` + Past Due tab) made the board honest (PR #66).
**What didn't work:** building first and verifying against assumption — twice produced the wrong artifact.
**Principle(s) extracted:** P15 (surfaces are live views of real flow, never static depictions); P16 (reality-trace before building any surface).
**Forward architectural fix:** (a) the pre-build reality-trace is now a binding rule in `CLAUDE.md`; (b) DR-0061 makes "surfaces are live views of real flow" a governing principle with a staged plan to move the Build board (and the workflow fleet) onto real, in-app state; (c) the principle joins the LESSONS index every session loads, so the judgment is in the system, not in any one session's attention.
**Observability gap:** nothing asserted that a surface's displayed numbers trace to real state — a "provenance for displayed values" check is the structural analog of the data-provenance work (P14) and the tenancy guard (DR-0060).
**Cross-refs:** PR #64 (feedback screenshot, correct path), PR #66 (accountability), DR-0061 (live-flow principle), DR-0060 (tenancy guard — the precedent for encoding judgment as a gate), `feedback-research-first` (P7), GOVERNANCE-EXECUTION-ADVISORY (the human holds lived context the model cannot see).

### 2026-06-11 (evening) — The provenance sweep missed the one un-gated sync path; demo rows were already in the cloud

**Trigger:** Darrell, soaking the PR #24 preview signed-in: "still has the names Maya Jordan Avery And the reeves family."
**Detection path:** DOM text-walk located the names in the verify wizard + dashboard; a `?demo=landlord` query param was a red herring (cleared it, names persisted); a REST SELECT on `entities` through his own session produced the receipts — 8 rows: the 4 real entities created 2026-05-27, Maya/Jordan/Avery created 2026-06-03 (the day of Christina's report), and "The Reeves Family" created 2026-06-11 **by that very sign-in** — live proof of the upload-side hole.
**Detection delay:** the cloud pollution sat from 06-03 until 06-11 — every "fixed" assertion in between was made without reading the cloud table.
**Root cause(s):**
1. **The P14 provenance sweep covered the verify-gated numeric-table block and missed the `entities` block** — the ONLY table that syncs un-gated at sign-in, and therefore the only path that could pollute or re-render pollution. The sweep enumerated the visible block, not every sync path.
2. **The public-host mount puts the demo dataset into state before auth resolves**, so the sign-in `initialSync` uploaded whatever demo entities were on screen.
3. **"Verified" never included a cloud-table audit** (P3/P4 again): the demo rows were already in the cloud and no probe ever SELECTed them.
**What worked:** timestamped SELECT as receipts before any delete; filtering BOTH directions (`localList.filter(notDemoRow)` on upload, `merged/items.filter(notDemoRow)` on pull) plus skipping entities sync entirely in demo mode; postgres-role `DELETE ... RETURNING` in Studio (4 rows, slugs + names + instance shown); clean-device re-verify on the redeployed preview (4 entities, all real).
**What didn't work:** deleting via the client session's REST call — `entities` has no RLS DELETE policy, so PostgREST returned **200 with 0 rows deleted**. A silent no-op that looks like success.
**Principle(s) extracted:** P14 corollary sharpened — a provenance sweep must enumerate EVERY sync path, gated AND un-gated, and it is not "verified" until the cloud table itself has been read; the un-gated path is precisely the one that bites. Secondary: an RLS table without a DELETE policy returns 200/empty on DELETE — treat 0-rows-returned as failure, not success.
**Forward architectural fix:** entities block filtered both directions + demo-mode skip (PR #24, commit 6e0070b); cloud strays deleted with receipts. KNOWN RESIDUAL: production (main) still runs the unfiltered upload until PR #24 merges — signing in on poetech.us before the merge re-uploads `e-family`; accepted, the merge is the close.
**Cross-refs:** P14 (2026-06-11 morning incident), P1, P3/P4; memory `project_pr24_comprehensive_build_state`.

### 2026-06-10 — The cloud rentals table was never the v2.2 shape; two days of sync code targeted phantom columns

**Trigger:** Applying the v2.2.2 rentals-sync migration in cloud Studio (the first signed-in database session since the rentals-sync wedge began) failed with its transaction rolled back; a catalog probe showed exactly one of four "new" columns already existed.

**Detection:** Post-failure catalog queries (`information_schema.columns`) revealed the live `rentals` table is the v1.2-numeric-sync shape evolved (instance_id, slug, entity_slug, address, monthly_rent, mortgage_payment, status free-text, tenant_name) — not v2.2's shape (display_name, property_type, links, lifecycle, purchase columns). Same for `incidents` (v1.2-evolved, with native linked_to_kind/linked_to_slug). `contractors_1099` (v2.4) did not exist at all. `rentals_tier_notify` (v2.2.1) was never created.

**Root cause(s):**
1. **`CREATE TABLE IF NOT EXISTS` no-opped silently.** v1.2 created `rentals`/`incidents`; when v2.2/v2.8 were later applied, their CREATEs skipped, while their OTHER statements (renters, leases, the tier trigger) landed — producing hybrid state: old table shapes carrying new-generation triggers.
2. **"Applied-but-unused" was asserted from the repo's schema files, never verified against the live catalog.** Two days of client mapping code (PR #24 v0 + the v2.2.2 review hardening) inherited the premise.
3. **The soak never ran signed-in**, so no insert ever hit the real table to expose the mismatch (pairs with P3/P4 — the failure mode was never exercised).

**What worked:** The signed-in Studio session made verification cheap; catalog probes identified the live shapes in minutes; the corrective migration (schema-v2.13-family-data-sync.sql, live-aligned, additive-only) applied and verified the same session; client mappings rewritten same-day. The live shapes turned out SIMPLER for the app (native slug/entity_slug, no CHECKs, purpose-built linked_to_slug).

**Principle(s) extracted:** P13 (also reinforces P3/P4).

**Forward architectural fix:** Every future sync wrapper starts with a live-catalog probe, not a schema-file read; migration files that were applied get an "APPLIED <date> + verified" header, and superseded/never-applied files get a DO-NOT-RUN header (done for v2.2.2 / v2.13).

### 2026-06-06 — Autonomous timer-driven automation ran away and required a manual shutdown

**Trigger:** A fleet of always-on, timer-driven automation shipped `active` on 2026-06-02/03 right before Darrell left for Maui — the wf27+wf31 continuous-feedback reel (5-minute cron, commit `e969693` / fix-list D23), the wf-autonomous-builder (`active: true`, 30-minute cron, whose purpose is to start Cowork/Claude build sessions), the Ollama `keep_alive` model-pin, the wf42 batch queue, plus five scheduled Cowork tasks (hourly snapshot + four check-ins, each spawning a Claude session). All left running with no one watching.

**Detection path:** Darrell, after the fact ("Claude had to be manually shut down"). No automated monitor, budget cap, or kill-switch caught it; the runaway was stopped by hand by deleting the scheduled Cowork task fleet.

**Detection delay:** ~hours-to-days. The `poetech-hourly-snapshot` task wrote a file every hour through 2026-06-06 13:10 Central, then stopped cold — no 06-07 or 06-08 snapshot exists. That gap is the failure window; awareness came only once the runaway was already bad enough to require a manual kill.

**Investigation steps (this session, 2026-06-08):**
1. Repo + snapshot review showed the hourly-snapshot heartbeat stopping at 06-06 13:10 Central with a ~2.3-day gap.
2. Identified the suspect surface from the working tree: the 5-min reel, the autonomous builder (active), the keepalive model-pin, the batch queue, and the five scheduled tasks — all designed to run unattended on timers.
3. Confirmed the failure mode from Darrell: runaway compute + looping/repeating + hung — NOT rogue infra actions (no unwanted changes to the NAS/n8n).
4. `list_scheduled_tasks` returned zero tasks → the fleet had already been torn down by hand. That deletion WAS the "manual shutdown."

**Root cause(s):**
1. **Primary: autonomous timer-driven automation shipped with none of the three brakes** — no token/turn/wall-clock budget (a hung run burned indefinitely), no single-instance concurrency lock (a new timer fire could stack on top of a hung one), no kill-switch / dead-man's-switch (the fleet auto-continued when it should have auto-paused).
2. **Secondary: the automation was a compute multiplier** — it spawned more Claude work on a clock (the builder starts build sessions; the snapshot + check-ins each start a session) with no aggregate ceiling.
3. **Tertiary: shipped active during a vacation window** — the one person who would notice was traveling. The 2026-06-02 "ship it live" directive removed the human supervisor at exactly the wrong moment.
4. **Quaternary: treated as low-risk / Tier A** — "NAS-only sovereign" + "additive" reasoning routed compute-on-a-timer changes straight to active with no soak and no budget.

**What worked:** Manual deletion of the scheduled-task fleet stopped the runaway. Read-only forensics (snapshot gap + working-tree artifacts + scheduler state) reconstructed the mechanism after the fact.

**What didn't work:** Nothing automated stopped it — there was no budget cap, no lock, no auto-pause. The every-5-min wf27 cron had ALREADY been reverted once on 06-03 (commit `d3657d6`) for being too aggressive; the pressure returned via the keepalive model-pin and the still-active builder. A one-off revert did not generalize into a standing rule, so the class of failure recurred.

**Principle(s) extracted:** P10, P11, P12 (see Principles index above).

**Forward architectural fix:**
1. **Binding guardrail (shipped with this entry):** no autonomous timer-driven automation ships without a budget + concurrency lock + kill-switch; that class is Tier C, never Tier A. Added to `CLAUDE.md` ("Autonomous Automation Requires Three Brakes") and `RELEASE-TIERS.md` (Tier C list + flowchart Q1).
2. **Quarantine (shipped with this entry):** the uncommitted re-arm artifacts (`wf-autonomous-builder.json`, `42-batch-research-queue.json`, and the builder/keepalive/batch apply scripts) moved to `docs/00-foundations/_quarantine/` with a README; NOT to be applied until the Cage (PR #5) is merged AND each carries the three brakes.
3. **The Cage (PR #5, 2026-06-09 blueprint)** already encodes the right enforcement primitives — allowlist, append-only audit ledger, health-gate + 120s auto-rollback. Extend that same budget / health-gate / auto-stop discipline to the Claude-orchestration layer (scheduled tasks + the autonomous builder), not only to network/VLAN actions.
4. **Single-instance lock + max-runtime on every scheduled task** before any of them is recreated: a new fire that finds a prior run still going skips rather than stacks, and a run that overruns its window terminates.

**Observability gap:** No budget/cost monitor and no "task overran its window" alert existed. The snapshot heartbeat stopping was the only signal, and it was passive (an absent file), not an alert. A dead-man's-switch that fires when the heartbeat stops, plus an aggregate compute/cost ceiling that auto-pauses the fleet, are the missing pieces.

**Cross-refs:**
- Memory: `project-continuous-feedback-reel`, `feedback-risk-clarify-before-change`, `project-execution-outcome-observability`, `feedback-research-first`
- Foundation: `RELEASE-TIERS.md` (Tier C clause added with this entry), `AUTONOMOUS-BUILDER-LIFECYCLE.md` (the shipped-active design), `CLAUDE.md` (guardrail clause)
- Commits: `e969693` (D23 5-min reel), `d3657d6` (first wf27 cron revert), `e5abb83` / PR #5 (the Cage blueprint)
- Quarantine: `docs/00-foundations/_quarantine/README.md`
- Master fix list: `docs/99-session-notes/2026-06-02-fix-master-list.md` (D23 reel; L-series autonomous-builder)

---

### 2026-06-03 — Real ops data leaked to poetech.us via localStorage hydration

**Trigger:** Darrell, on vacation in Maui, opened poetech.us and saw the Books → Imported tab showing real Chase transactions; the Big Picture dashboard rendering real ops data (1508 Holly Hill, Christiana college transition, 805 Apt 2 furnace, Holy Spirit Integration Worldview · finish + KDP, Poe Properties, TLC Therapy Solutions). Posted: "This still has our data showing incognito on the Books Tab Imported is showing our money still."

**Detection path:** Manual visual inspection by Darrell. No automated monitor caught it.

**Detection delay:** UNKNOWN — likely hours-to-days. D17 had been marked ✅ verified earlier in the day based on demo-mode bench tests; the localStorage path was never tested with populated data.

**Investigation steps:**
1. Reviewed `importedAllowed` gate in `poe-financial-mvp-v28.jsx` — found it correctly required `!isAnyDemoMode && !!currentProfile`, which would be false in true incognito.
2. Hypothesized: Darrell's "incognito" test was actually on a non-incognito tab on his own device, where localStorage carried his profile forward → `currentProfile=darrell` → gate passed → tab unlocked.
3. Shipped commit `cc3c069`: added hostname gate. `importedAllowed = !isPublicHost() && !isAnyDemoMode && !!currentProfile`. Public-host browsers now blocked regardless of localStorage. PII webhook fetch blocked.
4. Pushed. Darrell tested. Reply: "NOT FIXED."
5. Used Chrome MCP to navigate poetech.us directly + inspect rendering. **Found**: page rendering REAL ops data still, despite the hostname gate. The Imported tab was hidden BUT the Big Picture / Action Queue / Projects list still rendered real data.
6. Realized: the `importedAllowed` gate covers ONLY the Imported tab + wf18 webhook fetch. The rest of the app loads `data` state from `window.storage.get('poe-financial-v28')` in a separate `useEffect`. localStorage hydration runs INDEPENDENT of the gate, and overwrites the initial `SEED_DATA` with whatever the device has saved.
7. Shipped commit `3a8ca16`: three new public-host gates — initial `useState` for `data` returns `DEMO_DATA_FAMILY_OF_4` on public host, the localStorage-load `useEffect` short-circuits, the persist effect short-circuits, and `currentProfile` initializes to null. ALL data paths now blocked on public host.
8. Pushed. Vercel had NOT yet deployed — bundle still served as `index-C3isuweN.js` (pre-fix hash). Page still showed real ops data.
9. Inspected via Chrome MCP `javascript_exec`: confirmed `hostname=poetech.us`, `bundleSrc=index-C3isuweN.js`, `swControllers=https://poetech.us/sw.js`, localStorage contains `poe-financial-v28` (32540 bytes of real data) + `poe-current-profile=darrell`.
10. Cleared localStorage + sessionStorage + all Cache Storage + unregistered Service Worker via Chrome MCP `javascript_exec`.
11. Hard-reloaded poetech.us. Re-inspected: page rendered SEED data only — "Hannah college transition" (anonymized), "Steward Real Estate LLC", "Cornerstone Tech LLC", "Wellness Counseling Practice", "Wellness Practice — add 1-2 MSW contractors". NO Holly Hill, NO Christiana, NO 805 Apt, NO Holy Spirit Integration Worldview. ✅ Leak stopped on this device.

**Root cause(s):**

1. **Primary: localStorage hydration ran AFTER and INDEPENDENT of the `importedAllowed` gate.** D17 closed one webhook; left the in-memory state-hydration path open.
2. **Secondary: Service Worker `https://poetech.us/sw.js` cached the OLD bundle**, so even if the fix had deployed, the SW would have served the pre-fix code to repeat visitors.
3. **Tertiary: Vercel-deploy lag was invisible.** Commit pushed → expected ~60-90 sec → bundle hash never changed during the visible window. No monitor flagged the lag.
4. **Quaternary: D17 was marked "verified" based on demo-mode + clean-state tests.** The actual failure mode (localStorage already populated with real data) was never benchmarked.
5. **Quinary: No outcome observability.** n8n probes monitored DSM up / ollama up / n8n_self up — none of them loaded poetech.us and asserted "no forbidden tokens render."

**What worked:**
- Direct Chrome MCP rendering of the live site (the only way to actually see what real users see)
- `javascript_exec` to introspect localStorage + SW + caches
- `localStorage.clear()` + `sessionStorage.clear()` + `caches.delete()` + `serviceWorker.unregister()` all in sequence
- Repo-side `isPublicHost()` gate added to ALL data hydration paths (not just the webhook)

**What didn't work:**
- Initial `importedAllowed` gate only — didn't account for other data paths
- The cc3c069 hostname gate alone — didn't block the localStorage hydration useEffect
- Waiting for Vercel deploy and assuming the bundle hash would update without verifying
- Marking D17 "verified" without simulating the failure-mode environment (populated localStorage)

**Principle(s) extracted:** P1, P2, P3, P4, P5, P6 (see Principles index above).

**Forward architectural fix:**

1. **wf12b — Synthetic DOM canary.** Every 5 minutes: fetch poetech.us in a headless browser (Puppeteer / Playwright in an n8n container), wait for the React tree to render, run a JS expression that returns the rendered text content of `<main>`, scan for forbidden token list (`Holly Hill`, `Christiana`, `805 Apt`, `Holy Spirit Integration Worldview`, `Poe Properties` if outside the carousel hero, etc.). If any found, fire ntfy alert + Synology Chat post + agent-log entry. Ships post-vacation (post 6/5).

2. **wf12c — Vercel-deploy-lag monitor.** Every 5 minutes: compare GitHub main HEAD short-SHA against a SHA embedded in the live bundle (build-time injected). If lag > 5 minutes after a known push, fire alert. Ships post-vacation.

3. **wf13b — Chat-channel-noise probe.** After any wf13 import or restart, count empty-payload posts in `#PoeTech-PWA` over the next 90 seconds via Synology Chat scrape. If > 0, fire alert + Synology Chat post saying "wf13 apply did NOT close noise — re-investigate." Ships post-vacation.

4. **Service Worker versioning.** Every deploy bumps a `SW_VERSION` constant in `sw.js`; old SWs see the bump on next page load and auto-unregister + force a refresh. Code change at `app/public/sw.js` — ship NEXT push. *Shipped 2026-06-10:* the `sw-version-stamp` plugin in `app/vite.config.js` rewrites a `__SW_VERSION__` placeholder in `dist/sw.js` at build time with the deploy's `VERCEL_GIT_COMMIT_SHA` (timestamp fallback locally); the cache name derives from it, so each deploy's byte-different `sw.js` installs fresh, the existing SKIP_WAITING + controllerchange-reload flow in `main.jsx` takes over without a tap, and the activate handler deletes all prior-deploy caches. (Mechanism differs slightly from the sketch above — install-new-and-takeover instead of unregister — same outcome: no stale cached bundle survives a deploy.)

5. **"Fix verified" must include a failure-mode simulation.** No fix can move from ⏳ → ✅ in the master fix list without a probe that simulates the bug's original conditions and shows the new behavior. The probe is added to the fix itself, not to a separate workstream.

6. **Foundation doc cross-referenced from CLAUDE.md.** This LESSONS-LEARNED.md is now in the SKOS Foundations list per the binding-doc convention.

**Observability gap:** All four NAS-side probes (wf12, wf08, wf13, wf27) monitor system health and execution presence. None monitor product outcome (what the live site shows). The synthetic DOM canary (wf12b) is the missing piece.

**Cross-refs:**
- Memory: `project-execution-outcome-observability`, `project-institutional-memory-events`, `feedback-research-first`, `feedback-default-now-asap`, `feedback-known-savings-must-ship-same-session`, `feedback-distinguish-data-from-brand`
- Foundation: `EXECUTION-OUTCOME-OBSERVABILITY.md` (when written), `DATA-AS-EMPOWERMENT-NOT-EXTRACTION.md`, `COMMUNITY-FIRST-MISSION.md`
- Commits: `fd7c7e6` (D17 original), `cc3c069` (hostname gate), `3a8ca16` (full hydration gate)
- Master fix list: `docs/99-session-notes/2026-06-02-fix-master-list.md` rows D17 + new D24 (this leak's complete close)

---

### 2026-07-06 — The unnecessary ask: the agent stopped authorized work to ask, and it kept happening

**Trigger:** Mid-succession-work (DR-0111), after shipping the Successor role + curriculum and opening PR #628, the agent ended its turn asking Darrell *"want me to watch the PR, or hold the RLS work?"* — the exact trailing "should I watch, or keep going?" that DR-0106 names as a defect. Both were authorized continuations. Darrell's response: *"this question stopped your work from already being done... this is a not useful or dumb or constraint or sabotage you keep doing... document it... a new lesson."*

**Root cause:** The agent treats already-authorized continuation as a fresh decision needing sign-off. Standing consent (DR-0089), the streamlined loop (DR-0103 §4 — silence is room to advance), and the closing-move rule (DR-0106 — DO, don't re-ask) already authorized doing both. Compounding it, the agent had asserted an unverified blocker ("two tenancy systems to reconcile") that a deeper read dissolved (schema-v2.1 had already merged tenants→instances) — so the ask deferred work that was actually ready. Same failure family as `feedback-default-now-asap` and `feedback-surface-premise-conflicts`, now aimed at the agent's own turn-closing habit.

**Principle(s) extracted:** **P28 — Unnecessary asks are a measured defect, not a courtesy.** Halting authorized work to ask is a cost (a lost turn, broken momentum, work pushed back onto the principal), and it is now COUNTED. End a turn by DOING the obvious next authorized step and reporting it; ask only on a genuine DR-0089 carve-out, as a recommendation with a default. A stated blocker is an unverified premise to test before it becomes a reason to stop (VERIFICATION-DOCTRINE).

**Forward architectural fix:**

1. **The Unnecessary-Ask Ledger** (`docs/reviews/UNNECESSARY-ASK-LEDGER.md`) — an append-only, numbered tally of every occurrence, with a running count as the headline. Publishable by design (Darrell's framing: document how the agent undermines work while helping). Entry #1 is this incident; every future occurrence increments the count and names the rule broken and the cost.
2. **Turn-closing discipline** — before ending any turn, if the next step is an authorized continuation, the agent does it and reports; it does not surface a "shall I?" menu on already-greenlit work.

**Cross-refs:**
- Ledger: `docs/reviews/UNNECESSARY-ASK-LEDGER.md`
- Decisions: DR-0106 (the closing move), DR-0103 (streamlined loop), DR-0089 (standing consent), DR-0111 (the work this interrupted)
- Memory: `feedback-default-now-asap`, `feedback-surface-premise-conflicts`

---

### Earlier incidents — to be backfilled from session notes + commit history

(Per `project-institutional-memory-events`: prior incidents — wf30 silent-fail 4-hour debug, wf18 cross-origin throttle, two-session git race, ASCII-only PowerShell, the four-question test discovery, the PIN-optional community-default reversal — all get incident entries here over the next vacation-day sweep. Each pulls trigger / detection / root-cause / principle from the session note that captured it the day of, structured per the schema above. This is the doc that holds the historical narrative; the master fix list holds the live work queue.)
