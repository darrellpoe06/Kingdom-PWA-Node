# Auto-merge bootstrap — hands-off promotion to main, gated by branch protection.
#
# For PRs that TARGET main from feat/*, fix/*, merge/*, docs/*, or claude/*
# branches (claude/* is the remote/web session lane — added 2026-07-05 so agent
# PRs stop stalling until a human clicks merge),
# this enables GitHub's native auto-merge (squash). The PR then merges itself
# the moment the required status checks pass — and ONLY then. This never
# bypasses protection: auto-merge respects every required check and review
# rule on main, and a PR whose checks fail simply never merges.
#
# Auth is the workflow's own GITHUB_TOKEN (granted below). gh runs INSIDE the
# Action; nothing here calls gh from a local shell. Nothing here touches the
# protection rules themselves. No force, ever.
#
# WHY THE SWEEP (2026-06-16): the original version fired only on pull_request
# events. PR #192 — the PR that first turned the repo's "Allow auto-merge"
# setting ON — could not enable auto-merge on itself: the setting was OFF when
# its pull_request run fired, and the workflow never re-fired without a fresh
# push. Classic chicken-and-egg. The fix is to also run on check_suite:completed
# (a PR that went green BEFORE auto-merge was enabled now gets picked up when
# its checks finish, no push needed) and on workflow_dispatch (a manual sweep
# button), and to make the job SWEEP every eligible open PR rather than only the
# one in the triggering event's payload. Enabling auto-merge is idempotent, so
# a PR that already has it stays untouched.
#
# HOLD OPT-OUT: a PR labeled `hold` is skipped. Auto-merge is the default;
# holding is the explicit, human-governed exception — the gate for Tier C work
# that is intentionally soaking (RELEASE-TIERS.md) or awaiting family/Governor
# review. Remove the label when the soak/review is done and the next sweep (or
# any pull_request event) enables auto-merge.
#
# Pairs with ci.yml (the required checks this defers to) and RELEASE-TIERS.md.

name: Auto-merge

on:
  pull_request:
    # `closed` (2026-07-08, DR-0107 follow-through; CORRECTED same night): a
    # native auto-merge lands with NO subsequent event this workflow hears —
    # the GITHUB_TOKEN merge emits no push and main-CI doesn't run. `closed`
    # was added to make the merge run the deploy step below, but PROVED (PR
    # #697, watched) to be suppressed for auto-merges too: the close is a
    # GITHUB_TOKEN action, and GitHub suppresses workflow runs for every
    # event a GITHUB_TOKEN action emits — push, closed, all of it. `closed`
    # stays because it DOES fire for human-clicked merges/closes; for
    # auto-merges the real catcher is the armed-PR wait loop in the deploy
    # step (sized to span a full CI run), with the deploy-freshness cron
    # (best-effort, observed ~hourly) as the outer net.
    # `labeled` (2026-08-25, post-incident PR #1339): a `hold` label added AFTER
    # auto-merge was armed did nothing — native auto-merge ignores labels once
    # enabled, and the sweep only checked `hold` when ARMING. PR #1339 carried
    # the Governor's hold and merged anyway (the exact brake DR-0103 promises).
    # Now every labeling re-runs the sweep, and the sweep DISARMS any armed PR
    # that carries `hold` (step below) — the hand on the brake wins the race.
    types: [opened, reopened, ready_for_review, synchronize, closed, labeled, unlabeled]
    branches: [main]
  check_suite:
    types: [completed]
  workflow_dispatch: {}

permissions:
  contents: write
  pull-requests: write
  actions: write   # to dispatch db-migrate after a migration auto-merges

# Concurrency is SCOPED PER JOB, not workflow-wide (2026-07-19, P25/P26 fix).
# A workflow-level `cancel-in-progress: true` used to cancel the ENTIRE run when a
# newer sweep started — including the in-flight deploy/migrate wait-loop in the
# heal job — so during rapid successive merges the deploy dispatch was killed
# mid-poll and auto-merged commits sat undeployed (the exact gap that had to be
# hand-dispatched all through 2026-07-18 night). Now: the ENABLE job supersedes
# (only the latest matters — idempotent), while the HEAL job NEVER cancels (its
# wait-loop must run to completion to win the merge race — the 2026-07-14 lesson).

jobs:
  enable-auto-merge:
    name: enable auto-merge (squash) on eligible PRs
    runs-on: ubuntu-latest
    # Supersede: only the newest enable-sweep matters; enabling is idempotent.
    concurrency:
      group: auto-merge-enable
      cancel-in-progress: true
    steps:
      - name: Enable native auto-merge on every eligible open PR
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          # Eligible = open, non-draft, targets main, head branch is a release
          # lane (feat|fix|merge|docs|claude), NOT labeled `hold`, and auto-merge is NOT
          # already enabled. Filtering happens here (not in a job-level `if:`) so
          # the same job works uniformly across pull_request / check_suite /
          # workflow_dispatch — only pull_request events carry a single PR.
          mapfile -t prs < <(
            gh pr list --repo "$REPO" --state open --base main \
              --json number,headRefName,isDraft,autoMergeRequest,labels \
              --jq '.[]
                    | select(.isDraft == false)
                    | select(.headRefName | test("^(feat|fix|merge|docs|claude)/"))
                    | select(.autoMergeRequest == null)
                    | select(any(.labels[].name; . == "hold") | not)
                    | .number'
          )
          if [ "${#prs[@]}" -eq 0 ]; then
            echo "No eligible PRs awaiting auto-merge."
            exit 0
          fi
          echo "Eligible PRs: ${prs[*]}"
          for n in "${prs[@]}"; do
            echo "Enabling auto-merge (squash) on #$n"
            # Never fail the sweep on one PR: it may have merged or become
            # ineligible (e.g. a required check already failed) between the
            # list and the enable. Auto-merge still respects branch protection.
            if ! gh pr merge --repo "$REPO" --auto --squash "$n"; then
              echo "::warning::could not enable auto-merge on #$n (may have just merged or become ineligible)"
            fi
          done

      - name: DISARM auto-merge on every hold-labeled PR (the brake wins the race)
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          # Post-incident guard (PR #1339, 2026-08-25): native auto-merge armed
          # BEFORE a `hold` label lands keeps merging — labels never disarm it.
          # So every sweep also runs the reverse pass: any open PR to main that
          # carries `hold` AND has auto-merge armed gets DISARMED. Idempotent;
          # never fails the sweep on one PR.
          mapfile -t held < <(
            gh pr list --repo "$REPO" --state open --base main \
              --json number,autoMergeRequest,labels \
              --jq '.[]
                    | select(.autoMergeRequest != null)
                    | select(any(.labels[].name; . == "hold"))
                    | .number'
          )
          if [ "${#held[@]}" -eq 0 ]; then
            echo "No armed hold-labeled PRs to disarm."
            exit 0
          fi
          for n in "${held[@]}"; do
            echo "DISARMING auto-merge on held PR #$n"
            if ! gh pr merge --repo "$REPO" --disable-auto "$n"; then
              echo "::warning::could not disarm auto-merge on #$n"
            fi
          done

  # SPLIT INTO TWO JOBS (2026-08-28, the SIXTH miss — PR #1370). These were one
  # job with sequential steps, which coupled them in a way that could not be
  # tuned out: the migration poll always got the FIRST window and the deploy
  # poll only started when it expired. Every minute added to the migration
  # window was a minute the DEPLOY dispatch was late — and the deploy is the
  # uptime path, so the two goals were in direct competition inside one job.
  # The "re-check after the deploy wait" step below exists ONLY to paper over
  # that ordering; it stays as a settled-tip backstop, but it is no longer
  # load-bearing.
  #
  # Separate jobs run in PARALLEL with independent concurrency groups, so each
  # can poll its full window without delaying the other, and a slow migration
  # never holds the site on a stale build.
  heal-deploy:
    name: dispatch deploy for main's tip (GITHUB_TOKEN push gap)
    runs-on: ubuntu-latest
    # NON-cancelling: the wait-loop MUST run to completion to win the merge race
    # (the 2026-07-14 lesson). A newer enable-sweep must never cancel an
    # in-flight heal. Runs independently of enable (no `needs`).
    concurrency:
      group: auto-merge-heal-deploy
      cancel-in-progress: false
    steps:
      # SAME GITHUB_TOKEN gap, for PRODUCTION DEPLOY (2026-07-06). deploy-
      # cloudflare-pages.yml fires on push:main — which GitHub SUPPRESSES for the
      # GITHUB_TOKEN merge push (anti-recursion), exactly like db-migrate above.
      # So when auto-merge went live, every auto-merged PR landed on main but
      # NEVER deployed: poetech.us froze on the last HUMAN-merged build (6bfae29)
      # while main ran far ahead. workflow_dispatch by the token IS allowed, so
      # dispatch the deploy here whenever main's tip has not been deployed yet.
      # Gated on the deploy workflow's own last head_sha so a merge triggers
      # exactly ONE build (protects the CF Pages free build budget); a run already
      # queued/running for this same tip is a no-op. Deploy is single-flight
      # (its own concurrency group), so a redundant dispatch cannot stampede.
      #
      # WHY THE WAIT LOOP (2026-07-08, after the SECOND miss): this step RACES
      # the merge it exists to heal. The sweep fires on the green check_suite —
      # the same moment GitHub's native auto-merge performs the squash — so on
      # PR #685 (2026-07-07) and again PR #690 (2026-07-08) the check below ran
      # a beat BEFORE the merge landed, saw main's tip already deployed, and
      # exited; the merge push itself fires no event (token-suppressed), so the
      # heal never re-ran and both deploys had to be dispatched by hand. Fix:
      # when the tips match but ARMED PRs exist (auto-merge enabled, i.e. a
      # merge may be seconds away), poll for main to advance and dispatch the
      # moment it does. When nothing is armed, the old single check is
      # unchanged — no idle sweeps burn wait time.
      #
      # WHY THE WINDOW IS 6 MINUTES (2026-07-08, after the THIRD miss, PR
      # #697): the sweep that arms a fresh PR fires at PR-OPEN, when its CI
      # has only just started — the old 2-minute poll expired a beat before
      # the ~3-minute CI went green and the merge landed, and NO later event
      # fired (the auto-merge close is token-suppressed; see the `closed` note
      # at the top). 6 minutes spans a full CI run plus queue margin from the
      # earliest moment a sweep can arm a PR. Bounded cost: the poll runs only
      # while armed PRs exist, and a superseding sweep cancels it
      # (concurrency). CI failures exit via the armed PR never merging; the
      # cron remains the outer net.
      #
      # WHY IT IS NOW 12 MINUTES (2026-07-28, after the FOURTH miss, PR
      # #1093): the suite grew to ~6,700 tests and a full CI run is ~6
      # minutes — the sweep's poll expired at 22:46:35, ONE SECOND before
      # the merge landed at 22:46:36, and the deploy had to be dispatched by
      # hand. The window must track the real CI wall-clock, not the one it
      # had when the number was picked: 12 minutes is a full current CI run
      # plus equal margin. Same bounded cost as before (armed-PRs-only,
      # superseded sweeps cancel). The 5-minute freshness cron remains the
      # outer net, but GitHub throttles busy-repo schedules to ~hourly in
      # practice — the poll here is the net that actually catches the merge.
      # ONE HEAL USED TO SERVE EXACTLY ONE MERGE (2026-08-28, the SIXTH miss —
      # PR #1370). Measured on runs 3381 / 3388:
      #
      #   heal 3381  13:50:13 -> 14:06:51   dispatched db-migrate, exited
      #   heal 3388  14:01:39 -> 14:09:27   dispatched deploy 873, exited
      #   #1370 merged            14:11:25   nothing was watching
      #
      # Neither the deploy nor the migration fired for that merge, and both had
      # to be dispatched by hand — the site served a build older than main, and
      # 0159 sat unapplied under code that expected it.
      #
      # TWO THINGS COMPOUND, and neither is visible from one run's log:
      #   1. Both wait loops used to `exit 0` on their first dispatch. A heal
      #      therefore covered the FIRST merge in its window and abandoned the
      #      rest of the window it had already been granted.
      #   2. Only ONE job may be PENDING in a concurrency group — GitHub cancels
      #      the older pending one when a newer arrives. `cancel-in-progress:
      #      false` protects the RUNNING heal and nothing else, so during a
      #      burst every sweep in between is cancelled while queued (runs 3379,
      #      3383, 3385, 3387 today, each cancelled within seconds).
      #
      # Together: in a burst, one heal serves one merge and every later merge is
      # orphaned until a cron that this repo delivers ~7.7% of the time.
      #
      # The fix is (1) — keep polling for the rest of the window instead of
      # exiting, so one heal covers every merge inside it. Cheap because both
      # dispatches are sha-gated: a repeat for the same tip is a no-op. (2) is
      # GitHub's scheduler and cannot be configured away; making each surviving
      # heal cover more merges is what actually shrinks the exposure.
      - name: Deploy main's tip if it isn't the last-deployed commit (closes the GITHUB_TOKEN push gap for CF Pages)
        if: ${{ always() }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          dispatched_for=""
          deploy_if_stale() {
            main_sha=$(gh api "repos/$REPO/commits/main" --jq '.sha' 2>/dev/null || echo "")
            last_deployed=$(gh api "repos/$REPO/actions/workflows/deploy-cloudflare-pages.yml/runs?per_page=1" \
              --jq '.workflow_runs[0].head_sha // ""' 2>/dev/null || echo "")
            # `dispatched_for` (2026-08-28): the loop no longer exits after a
            # dispatch, so it can come back around before the new run is listed.
            # Without this it would dispatch the same tip twice and burn a CF
            # Pages build for nothing.
            if [ -n "$main_sha" ] && [ "$main_sha" != "$last_deployed" ] && [ "$main_sha" != "$dispatched_for" ]; then
              echo "main tip $main_sha not yet deployed (last deployed $last_deployed) — dispatching CF Pages deploy."
              gh workflow run deploy-cloudflare-pages.yml --repo "$REPO" --ref main \
                || echo "::warning::could not dispatch deploy (it can be run manually)"
              dispatched_for="$main_sha"
              return 0
            fi
            return 1
          }
          # DO NOT EXIT ON THE FIRST DISPATCH — see the burst note above the
          # step. `dispatched_for` makes a repeat for the same tip a no-op, so
          # continuing to poll costs nothing and covers the NEXT merge too.
          deploy_if_stale || true
          armed=$(gh pr list --repo "$REPO" --state open --base main \
            --json autoMergeRequest --jq '[.[] | select(.autoMergeRequest != null)] | length' 2>/dev/null || echo 0)
          if [ "$armed" -gt 0 ]; then
            echo "$armed armed PR(s) may merge momentarily — polling up to 12 minutes."
            for i in $(seq 1 48); do
              sleep 15
              deploy_if_stale || true
            done
            echo "Poll window closed; the next sweep or the scheduled freshness check covers what is left."
          else
            echo "main tip already deployed and no armed PRs; nothing to dispatch."
          fi

  heal-migrate:
    name: dispatch migrations for main's tip (GITHUB_TOKEN push gap)
    runs-on: ubuntu-latest
    concurrency:
      group: auto-merge-heal-migrate
      cancel-in-progress: false
    steps:
      - name: Apply migrations if main's tip added one (closes the GITHUB_TOKEN push gap)
        if: ${{ always() }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          dispatched_for=""
          migrate_if_tip_migration() {
            main_sha=$(gh api "repos/$REPO/commits/main" --jq '.sha' 2>/dev/null || echo "")
            [ -n "$main_sha" ] || return 1
            [ "$main_sha" != "$dispatched_for" ] || return 1
            touched=$(gh api "repos/$REPO/commits/$main_sha" \
              --jq 'any(.files[]?.filename; startswith("infra/supabase/migrations-auto/"))' 2>/dev/null || echo false)
            if [ "$touched" = "true" ]; then
              echo "main tip $main_sha touched a migration — dispatching db-migrate."
              gh workflow run db-migrate.yml --repo "$REPO" --ref main \
                || echo "::warning::could not dispatch db-migrate (it can be run manually)"
              dispatched_for="$main_sha"
              return 0
            fi
            return 1
          }
          # DO NOT EXIT ON THE FIRST DISPATCH (2026-08-28, the SIXTH miss —
          # #1370). See the burst note on the deploy step below: one heal used
          # to serve exactly one merge, and every merge that landed after its
          # dispatch had nothing watching. `dispatched_for` already makes a
          # repeat dispatch for the same tip a no-op, so polling on is free.
          migrate_if_tip_migration || true
          armed=$(gh pr list --repo "$REPO" --state open --base main \
            --json autoMergeRequest --jq '[.[] | select(.autoMergeRequest != null)] | length' 2>/dev/null || echo 0)
          if [ "$armed" -gt 0 ]; then
            echo "$armed armed PR(s) may merge momentarily — polling up to 12 minutes."
            for i in $(seq 1 48); do
              sleep 15
              migrate_if_tip_migration || true
            done
            echo "Poll window closed; the re-check step and migrate-freshness cover what is left."
          else
            echo "No armed PRs; nothing more to watch for."
          fi

      # THE ORDERING HOLE, CLOSED (2026-08-28, after the FIFTH miss — PR #1361).
      # The two waits above are SEQUENTIAL steps in one job, so the migration
      # poll always gets the FIRST window and the deploy poll a later, second
      # one. Measured on run 33137488317:
      #
      #   migration poll   02:58:03 -> 03:04:30   (6m27s, gave up)
      #   deploy poll      03:04:30 -> 03:06:56   (caught the merge)
      #   the merge                   03:06:50
      #
      # So the deploy landed and the migration did not — the worst shape there
      # is: #1361's new code went live against a database that never got its
      # schema (a Systems tab with no tables, and a home-is-never-listed CHECK
      # that did not exist). Both had to be dispatched by hand.
      #
      # This is the P26 class, twice over. The deploy step's own window was
      # raised from 6 to 12 minutes in July, after PR #1093 taught that 6 no
      # longer spans a real CI run — and the migration step, sixty lines up in
      # the same file, was left at 6. The fix was already here.
      #
      # Raising the first window is the WRONG fix: it would delay the deploy
      # step by up to twelve minutes on every sweep. Instead the migration
      # question is asked ONCE MORE, after the deploy poll has resolved — by
      # which time main's tip is settled, so no polling is needed. Between the
      # two checks the whole span is covered, and migrate-freshness.yml (the
      # 5-minute cron) is the outer net for the last merge in a quiet stretch,
      # which no poll window in a PR-event workflow can ever reach.
      #
      # db-migrate is idempotent and single-flight, so a dispatch here that
      # duplicates the earlier one is harmless by design.
      - name: Re-check for a migration once the deploy wait has resolved (the ordering hole)
        if: ${{ always() }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          main_sha=$(gh api "repos/$REPO/commits/main" --jq '.sha' 2>/dev/null || echo "")
          if [ -z "$main_sha" ]; then
            echo "::warning::could not read main's tip; the freshness cron covers this."
            exit 0
          fi
          last_sha=$(gh api "repos/$REPO/actions/workflows/db-migrate.yml/runs?status=success&per_page=1" \
            --jq '.workflow_runs[0].head_sha // ""' 2>/dev/null || echo "")
          if [ "$main_sha" = "$last_sha" ]; then
            echo "db-migrate already ran against main's tip ($main_sha); nothing to re-dispatch."
            exit 0
          fi
          touched=$(gh api "repos/$REPO/commits/$main_sha" \
            --jq 'any(.files[]?.filename; startswith("infra/supabase/migrations-auto/"))' 2>/dev/null || echo "unknown")
          if [ "$touched" = "true" ]; then
            echo "main tip $main_sha carries a migration db-migrate has not run — dispatching."
            gh workflow run db-migrate.yml --repo "$REPO" --ref main \
              || echo "::warning::could not dispatch db-migrate (migrate-freshness covers it)"
          elif [ "$touched" = "unknown" ]; then
            # Unknown freshness never reads as fresh (DR-0076 §8).
            echo "::warning::could not tell whether main's tip carries a migration — leaving it to migrate-freshness."
          else
            echo "main tip $main_sha carries no migration change; nothing to apply."
          fi
